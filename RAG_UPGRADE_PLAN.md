# RAG_UPGRADE_PLAN.md — RAG 优化与问答助手建设流程

> 状态：**执行中（阶段 0-5 已完成，2026-08）**
> 目标：把现在"只能搜片段"的 RAG 升级成**真正有用的问答助手**（检索 → 精排 → 带引用生成答案）
> 前置阅读：`AGENTS.md`（项目纪律）· `PROJECT_STATE.md`（现状）· `scripts/README.md`（工具清单）
> 配套能力：在线 LLM 接口（阶段 0），密钥走 `.env` / 环境变量，绝不写死在代码里
> 📓 **开发决策与踩坑记录见 `RAG_DEVLOG.md`**（为什么这么设计、遇到过什么问题、怎么解决的）

---

## ✅ 当前进度（2026-08 更新）

| 阶段 | 内容 | 状态 | 关键结果 |
|---|---|---|---|
| 0 | 在线 LLM 接口 + 密钥安全 | ✅ 完成 | `scripts/llm_client.py`；`.gitignore` + `.env.example`；deepseek-chat 真实调用成功 |
| 1 | 评测集 + 基线 | ✅ 完成 | `output/eval/eval_set.jsonl`（71 条，5 意图×3 难度，LLM 自动生成+配方库核对）；基线 Recall@5=72% |
| 2 | 意图识别分层 | ✅ 完成 | `scripts/intent_router.py`（L1 规则 + L3 LLM 兜底） |
| 3 | 结构化直查路由 | ✅ 完成 | `scripts/rag_ask.py`：配方/设备/歧义/设备产物反查 |
| 4 | 检索增强 | ✅ 完成 | jieba 词典（1789 词）+ chunk 元信息前缀 → **Recall@5 72%→85%**（数值类 24%→81%） |
| 5 | 答案生成 API | ✅ 完成 | `POST /api/ask`：意图→路由→检索→LLM 带 `[来源N]` 引用回答，低相关诚实拒答 |
| 前端 | RAG 问答接入网页 | ✅ 完成 | `web/index.html` 双模式（合成树/知识问答） |
| 前端 | 终末地 UI | ✅ 完成 | 该阶段曾完成深色 HUD；当前已迭代为白色工业制图 v3 与纵向图片配方树 |
| 评测 | 迭代优化 | ✅ 完成 | 名称召回 + 长条目漏索引修复 + gold 审计 → **Recall@5 100%、MRR 97.3%**（n=71） |

**评测迭代记录**（output/eval/）：基线 72% → 词典+元信息 85% → 新评测集 89% → 系列判定 93% → 名称召回与索引完整性修复 100%。

**剩余可选项**：rerank 精排（cross-encoder，可进一步提升排序质量）、双实体查询召回优化（困难比较类）、评测集人工抽检修正。

---

## 一、为什么现在不够"有用"（基线体检）

| # | 现状问题 | 影响 |
|---|----------|------|
| 1 | 没有意图识别，所有问题走同一条检索管线 | "怎么造"（应精确查配方）和"是什么"（应语义搜知识库）互相干扰 |
| 2 | 评测集只有 6 条查询，且全是配方类 | 指标无法代表真实使用，无法支撑优化决策 |
| 3 | 命中判定用子串匹配（`t in name`） | 指标虚高（查"电池"命中"中容谷地电池"就算对） |
| 4 | 检索完只返回片段，没有答案生成 | 用户要自己拼答案，不算"问答助手" |
| 5 | RRF k 值、双路 top_n 拍脑袋；jieba 无自定义词典 | 专有名词（重息壤/赫铜零件）被切碎，参数未调优 |

---

## 二、优化后的总体架构

```
用户问题
  │
  ▼
① 意图识别（分层漏斗，从便宜到贵）
   L1 规则层（关键词/正则，毫秒级）
   L2 轻量分类器（未命中时，可选）
   L3 在线 LLM 兜底（低置信度时，慢但准）
  │
  ▼
② 检索路由（按意图走不同通道）
   ├─ 配方类  → 结构化配方库直查 recipes.json（精确，命中率 100%）
   ├─ 知识类  → 混合检索（向量 + BM25 → RRF）+ 分类过滤
   ├─ 数值类  → 混合检索 + 限定分类 metadata 过滤
   └─ 比较类  → 双方分别检索后合并
  │
  ▼
③ 精排（cross-encoder Reranker，如 bge-reranker-v2-m3）
   召回 top-50 → 逐对打分 → 取 top-5
  │
  ▼
④ 答案生成（在线 LLM）
   检索片段 + 引用溯源 → 生成带 [1][2] 引用的回答；检索分低 → 诚实拒答
  │
  ▼
⑤ 前端展示（搜索框 → 问答卡片 / 合成树，引用可点击跳转）
```

---

## 三、分阶段执行流程

### 阶段 0：基础设施 — 在线 LLM 接口（约 1 天）

**为什么先做**：后面的意图识别兜底、答案生成、测试集生成、评测裁判全都依赖它。

| 交付物 | 说明 |
|---|---|
| `scripts/llm_client.py` | 统一客户端：`chat()` 普通对话 / `chat_json()` 强制 JSON（意图识别、测试集生成用）/ `available()` 健康检查。走 OpenAI 兼容协议（DeepSeek/通义/Kimi 通用），用已装的 `httpx`，不新增依赖 |
| `.gitignore` | ⚠️ 项目目前没有！补上，把 `.env`、日志、缓存排除出 git，防止密钥入库 |
| `.env.example` | 密钥占位模板（`LLM_API_KEY=` 空值），可安全提交 |

**密钥安全规则**：
- key 只从环境变量 / `.env` 读取（`api_server.py` 已有 `_load_env()`，直接复用约定）
- 代码零明文 key；错误信息不打印 key；日志不记录 key
- 未配 key 时 `available()` 返回 False，调用方优雅降级（如退回"只给片段"），不崩

**验收**：三个方法可调用；未配 key 不报错；`.env` 已进 `.gitignore`。

---

### 阶段 1：评测集建设（约 2-3 天）← 全项目最重要的尺子

**原则**：没有考卷就没有优化，也没有验收。先建尺子，后面每步优化都跑同一套题。

**1.1 测试集结构**：意图类别 × 难度 二维矩阵，80+ 条起步

| 意图类别 | 简单（字面匹配） | 中等（同义改写） | 困难（口语化描述） |
|---|---|---|---|
| 配方类（怎么造） | "赫铜零件怎么合成" | "铁零件怎么来的" | "什么机器能做电池" |
| 设备类（能干什么） | "天有洪炉有什么用" | "什么设备能辅助攻击" | "能装水的罐子叫什么" |
| 知识类（是什么） | "重息壤是什么" | "介绍下重息壤" | "这玩意儿是干嘛的" |
| 比较类（哪个好） | "A和B哪个强" | "A和B有啥区别" | "选哪个好" |
| 数值类（多少级） | "XX满级属性" | "XX技能倍率" | "它强不强" |

每格 ≥3 条 → 5 类 × 3 难度 × 3 ≈ 45 条打底，再按实际类别占比扩到 80-100 条。

**1.2 每条标注格式**（存 `output/eval/`，JSONL）：
```json
{
  "query": "什么机器能做电池",
  "intent": "配方",
  "difficulty": "困难",
  "gold_ids": ["中容谷地电池-item-id"],   // 正确答案的条目/物品 id
  "relevance": 2                           // 0 无关 / 1 部分相关 / 2 完全相关
}
```

**1.3 标注来源**：
- 配方/设备类 → 用结构化 `recipes.json` 交叉验证（gold 天然权威，还能自动核对）
- 知识类 → 在线 LLM 起草候选 + 人工抽检
- 负例挖掘：把"检索常排前面但其实是错的"条目记为 hard negative，供后续调优

**1.4 指标**：`Recall@k` / `MRR` / `NDCG@k` / `Precision@k`，**按意图类别 × 难度分别报数**（一眼看出哪类问题烂在哪）。

**验收**：基线评测跑通，输出 `output/eval/baseline.json` 存档（当前水平 ≈ Recall@5=66.7%，且只有配方类有数）。

---

### 阶段 2：意图识别分层（约 2 天）

**1.1 分层漏斗**（从便宜到贵，低置信度才往下走）：

```
查询进来
  ↓
L1 规则层（毫秒级，零成本）
   "怎么造/合成/配方/需要什么材料"  → 配方类
   "什么设备/能干什么/有什么用"      → 设备类
   "是什么/介绍/背景/档案"           → 知识类
   "A和B哪个好/区别"                → 比较类
   "多少级/数值/属性/倍率"           → 数值类
  ↓ 未命中
L2 轻量分类（可选：bge 系列分类模型，本地离线）
   打意图标签 + 置信度
  ↓ 置信度低
L3 在线 LLM 兜底（阶段 0 的 chat_json）
   "判断这句话的意图，只输出 JSON"
```

**1.2 意图 → 检索策略映射**（这是"有用"的关键）：

| 意图 | 检索通道 | 答案形态 |
|---|---|---|
| 配方 | 结构化配方库直查（不走语义检索） | 配方卡/合成树 |
| 设备 | 知识库混合检索 + 设备分类过滤 | 介绍 + 配方卡 |
| 知识 | 知识库混合检索 | 带引用介绍 |
| 比较 | 双方分别检索后合并 | 对比表 |
| 数值 | 知识库检索 + 分类过滤 | 数值摘录 + 引用 |

**验收**：测试集意图分类准确率 ≥90%（L1+L3 组合即可，L2 视效果决定是否保留）。

---

### 阶段 3：检索路由 — 结构化直查（约 1 天）← 咱项目的王牌

网上 RAG 方案默认只有非结构化文档，**咱手里有 345 个精确配方的 `recipes.json`**，这是别人做梦都想要的。

- "怎么造 X" → 直接 `find_item_ids_by_name(X)` 查配方库，歧义 → 返回候选列表（复用 `/api/synthesis` 现有逻辑）
- 命中率从"语义猜"变成"精确查"，配方类 100% 命中

**验收**：配方类测试集命中率 100%（与结构化库对照一致）。

---

### 阶段 4：检索增强（约 3-5 天，每小步独立验证）

| 小步 | 手段 | 原理 | 优先级 |
|---|---|---|---|
| 4.1 | **jieba 自定义词典**：把游戏专有名词（重息壤/赫铜零件/中容谷地电池…）加进词典 | 防止专有名词被切碎导致 BM25 失配 | 高（便宜） |
| 4.2 | **chunk 元信息拼进检索文本**：`【分类】名称：正文`（Contextual Retrieval 轻量版） | 切块丢失语境 → 把条目级元信息补回去，在线 LLM 版成本高，规则版零成本 | 高（便宜） |
| 4.3 | **BGE Reranker 精排**：召回 top-50 → cross-encoder 逐对打分 → top-5 | 语义精排，网上验证的单一最大提升点；`bge-reranker-v2-m3` 可本地离线 | 高 |
| 4.4 | **同义词表 + 实体归一**：合成=制造=加工；"电池"→"中容谷地电池"（复用 `find_item_ids_by_name`） | 治"同义改写查不到" | 中 |
| 4.5 | **metadata 过滤**：知识类问题只搜对应分类（ChromaDB 原生支持） | 排掉干扰项 | 低（顺手做） |
| 4.6 | **参数调优**：网格搜索 RRF 的 k、双路 top_n（测试集驱动） | 白捡几个点 | 低 |

**纪律**：每小步做完跑全套评测集，**指标只允许升不允许降**，降了就回退该小步并记录原因。

---

### 阶段 5：答案生成（约 2-3 天）← 从"搜片段"变"真问答"的分水岭

- 检索 top-5 片段 + 在线 LLM → 生成带 `[1][2]` 引用的回答
- **拒答策略**：检索相关度低于阈值时，诚实说"知识库里没找到相关材料"，不编造
- 新增 `GET /api/ask?q=...` 端点（复用阶段 0 的 `llm_client`）
- 前端接入：搜索框 → 问答卡片（引用可点击跳转到 wiki 条目）

**验收**：拿测试集 20 条抽样人工打分：回答有用性（忠实度 + 完整性 + 引用正确性）平均 ≥4/5。

---

### 阶段 6：可选进阶（按需，不强制）

| 项 | 说明 | 触发条件 |
|---|---|---|
| RAGAS 评测 | 用在线 LLM 当裁判，跑 Faithfulness / Context Precision / Context Recall 四大指标 | 阶段 1-5 全部完成后有余力 |
| 父-子分块 | 小 chunk 检索、大 chunk 喂 LLM，适合干员档案长文 | 知识类 Recall 仍不达标时 |
| embedding 升级 | bge-small(384维) → bge-base/large-zh | 语义召回仍是瓶颈且离线缓存可用时 |
| 多轮对话 | 查询改写带入上下文（"它"指代上一轮物品） | 问答助手交互进入深水区时 |

---

## 四、回归纪律（贯穿始终）

1. 每阶段结束：跑全套评测集，**指标只升不降**，结果存档 `output/eval/`
2. 新增真实用户问题 → 立即补进测试集
3. 数据更新（重新抓 WIKI）→ `build_rag.py --incremental` 增量重建，再跑回归
4. 所有改动只落在 `scripts/ web/ endfield_kb/ output/ logs/` + 本计划文档

---

## 五、风险与对策

| 风险 | 对策 |
|---|---|
| 在线 LLM 调用超时/限流 | `llm_client` 设超时 + 重试 + 降级（退回纯检索） |
| 密钥泄露 | `.gitignore` 排除 `.env`；密钥只在环境变量，不打印不记录 |
| 评测集主观性 | gold 优先用结构化配方库自动核对；知识类人工抽检 |
| 优化陷入"指标好看但实际没用" | 每阶段末抽样 20 条人工打分兜底 |
| 离线模型缓存缺失 | 先确认 bge-reranker 已离线缓存，否则先下好再启用 |

---

## 六、执行顺序总览

| 阶段 | 内容 | 预计 | 依赖 |
|---|---|---|---|
| 0 | 在线 LLM 接口 + 密钥安全 | 1 天 | 无 |
| 1 | 评测集建设（80+ 条） | 2-3 天 | 无 |
| 2 | 意图识别分层 | 2 天 | 阶段 0（L3 兜底） |
| 3 | 检索路由（配方直查） | 1 天 | 阶段 2 |
| 4 | 检索增强（词典/元信息/rerank/改写） | 3-5 天 | 阶段 1（尺子） |
| 5 | 答案生成 + 引用 | 2-3 天 | 阶段 0、4 |
| 6 | 可选进阶 | 按需 | 阶段 1-5 |

> 总体约 2 周可到"真有用"状态，每阶段可独立交付、独立验收。
