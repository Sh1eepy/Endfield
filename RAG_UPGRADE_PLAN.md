# RAG 改进路线

> 主体功能已经完成。这份文件只列保留项和启用条件，不再重复现有架构。

## 已完成

- OpenAI 兼容 LLM 客户端和 `.env` 安全配置；
- 规则优先、LLM 兜底的意图识别；
- 名称 + BM25 + 向量混合检索；
- 专名词典、chunk 元信息前缀和漏索引修复；
- 实体直取、分类枚举、mention、关键词和查询改写；
- 轻量知识图谱、图检索、增量更新和关系审查；
- 深度健康、运行指标、答案评测和 CI 质量门禁；
- 长文档查询聚焦，避免固定截取正文开头。
- 有 sections 的长条目全文补漏，以及干员中文语音台词入库；
- 一次受约束的语义检索规划，替代低置信意图分类与查询改写的重复调用；
- 索引策略版本 hash 与 Chroma 分批写入。

## 下一步

1. **扩充困难评测集**：收集真实失败问题，标注必要事实、来源、子问题和正确拒答。
2. **补运行 trace**：记录分类、检索、图查询、改写和生成耗时，区分 LLM 错误类型。
3. **判断是否需要补检索循环**：只有第一轮证据覆盖不足是主要错误时才实现，最多补一次。
4. **判断是否需要 reranker**：先加入 hard negatives；只有首位排序和引用明显改善时启用。
5. **补生产监控**：部署到多实例环境后再接持久化指标和报警。

## 新能力的上线条件

- 固定评测集上的事实正确率不下降；
- 答案完整性有明确提升；
- 正确拒答率不明显下降；
- 延迟增加在可接受范围；
- 有超时、次数上限、日志和降级方案；
- `quality_gate.py` 通过。

准确率和稳定性优先于小幅成本节省，但不接受没有评测收益的额外复杂度。
