# 底层 Agent 与工作流设计

> 更新日期：2026-08。本文回答三件事：**这个系统的"Agent"到底是什么、为什么这么设计、踩过哪些坑**。
> 它是 `KNOWLEDGE_SYSTEM_ARCHITECTURE.md`（现状）、`RAG_DEVLOG.md`（决策记录）、`DEPLOYMENT.md`（运维）与
> `tests/`（验收）的"设计理由"合订本：架构文档讲"是什么"，本文讲"为什么"。
>
> 阅读建议：先看 §1 现状快照，再看 §2 核心概念（一句话理解这个 Agent），§3.3 是线上质量闭环，
> §4 是 Agent Loop 设计重点，§8 是踩坑索引（可直接检索），§9 是给别人项目可搬走的经验。

---

## 1. 项目整体进程（初步说明）

### 1.1 一句话

用户输入物品/设备名（搜索框模糊联想）→ 前端 React SVG 配方合成树（叶子收敛到基础资源）；
无配方物品回退知识库显示其信息；知识问答模式由"确定性编排管线 + 可选 LLM"回答。

### 1.2 当前进度快照

| 维度 | 状态 | 证据 |
|---|---|---|
| 数据 | ✅ 1958 条按 22 分类知识库、345 个真实配方、31 名干员详情、媒体结构库 | `endfield_kb/`、`output/recipes.json` |
| 检索 | ✅ RAG 7319 chunks（向量 + 分类 BM25）、mention 反查、全文关键词 | `output/rag/` |
| 图谱 | ✅ 2129 实体 / 9358 关系，带来源与原文证据，增量更新 | `output/knowledge_graph/graph.db` |
| 问答 | ✅ 路由漏斗（枚举/直查/图/多路 RAG）+ 可选 LLM 生成带引用回答 | `scripts/rag_ask.py` |
| 评测 | ✅ 71 条检索集 Recall@5=100%、MRR=97.3%；图评测 10/10；关系审查 1656/1656 | `output/eval/` |
| 可观测与坏例闭环 | ✅ 脱敏 Trace、LLM token usage、Web/小程序反馈入口、隔离审核、Replay 与建议归因 | `scripts/rag_trace.py`、`scripts/replay_bad_cases.py` |
| 验收 | ✅ 54 个基础单测 + 34 个路由/安全/Trace 专项测试 + Web/小程序测试 + CI 质量门禁 | `tests/`、`scripts/test_*.py` |
| 发布 | ✅ Docker 镜像本地构建与容器运行验证通过；Compose/Nginx/HTTPS/回滚手册齐备 | `DEPLOYMENT.md` |
| 待办 | ⏳ 公网部署（需服务器权限）；小程序真机验收；困难集积累与 Judge 校准；reranker / 受限补检索循环**未经评测证明收益前不引入** | `KNOWLEDGE_SYSTEM_ARCHITECTURE.md`（路线图） |

### 1.3 发布边界（为什么长这样）

- 服务默认单 worker（每个 worker 会独立加载 embedding 模型与索引，小内存服务器扛不住多份）；
- 公网只开 80/443，容器只绑 `127.0.0.1:8000`；Nginx 与应用层 SQLite 共同限制问答频率和额度；
- `/api/health/deep` 与 `/api/metrics` 只允许回环客户端或有效管理令牌访问；
- 问答 Trace 与请求额度库放在持久卷中，部署更新不清零；包含用户主动反馈正文的数据库按用户内容保护。

---

## 2. 核心概念：这个系统的"Agent"是什么

**它不是开放式 Agent，而是"确定性编排管线 + 可选 LLM 子步骤"。**

```text
query ──► 规则/正则（零成本）
           ├─ 命中配方/设备 → 结构化直查（0 次 LLM）
           ├─ 命中枚举 → 分类过滤（0～1 次 LLM）
           └─ 未命中 → 一次受约束语义规划（1 次 LLM）
                        ├─ 关系/多跳 → 图检索 + 原文证据 → 生成（1 次 LLM）
                        ├─ 开放问题 → 多路检索 → 生成（1 次 LLM）
                        └─ 低相关 → 诚实拒答（0 次 LLM）
```

| 路径 | 典型 LLM 次数 | 说明 |
|---|---|---|
| 配方/设备直查且不生成 | 0 | 完全结构化，LLM 不参与 |
| 结构化结果文本整理 | 0～1 | 仅需要自然语言时才生成 |
| 普通开放 RAG | 2 | 语义检索规划 1 次 + 最终回答 1 次 |
| 图关系且路径明确 | 1～2 | 可规则直达；需消歧时先规划 |
| 失败 / 未配置 LLM | 更少 | 规则降级路径兜底 |

**为什么这么设计（§4 详述）**：目标优先级是「事实准确和证据可追溯 → 失败可见和稳定可复现 → 答案完整 →
延迟成本受控 → 表达自然」。一个"什么都能干"的开放式 Agent 在单领域知识问答里既不更快、也不更准，
只会引入不可复现路径和更大的错误面；所以这里把"确定性"当默认，把"LLM"当受约束的零件。

---

## 3. 整体工作流设计

### 3.1 离线构建链路（数据管道）

```text
官方 WIKI 块式 JSON（147MB，只读事实源）
  ├─ build_kb_all.py      → endfield_kb/（22 分类 jsonl+md，sections + full_text + 结构化块）
  │    ├─ build_rag.py    → output/rag/：Chroma 向量 + 分类 BM25 分片 + chunks.json manifest
  │    ├─ mention_index   → output/mention_index.json（"谁提到了 X"反查）
  │    └─ build_knowledge_graph.py → graph.db（实体/关系/证据/来源哈希）
  ├─ build_operator_details.py → 干员详情（中文语音台词单独进 RAG）
  ├─ extract_media.py     → output/item_media.json（封面/正文图/外链/引用）
  └─ recipe_extract.py    → output/recipes.json（345 配方，合成树唯一数据源）
```

设计要点（每个都是踩坑换来的，详见 §8）：

- **数据分层**：原始 WIKI JSON 是只读事实源；`endfield_kb/`、`output/` 全部是**可重建产物**。
  这决定了"索引坏了就重建，不手改"的恢复策略（§7）。
- **切片规则**：短条目整条入库；超长按 `sections` 拆；`sections` 缺失回退切 `full_text`；
  `sections` 未覆盖的 `full_text` 内容补索引（防静默漏索引）。
- **chunk 前缀**：`【分类】条目名：条目名。正文` —— 名称同时进入 BM25 与 embedding，
  短名称条目（如"负山"）也能被名称命中。
- **专名词典**：`gen_jieba_dict.py` 扫描配方库+知识库名称生成 `dict_zh.txt`，防止 jieba
  把"重息壤/向心之引"切碎导致 BM25 失配。
- **增量机制**：条目级 `content_hash`（正文 + sections + **索引策略版本** md5）写入 manifest →
  对比后只重 embedding 变更条目、只重建变更分类的 BM25 分片；BM25 分片缺失/陈旧/损坏自动自愈。

### 3.2 在线问答工作流（一个请求的完整旅程）

```text
POST /api/ask {query, top_k, gen_answer}
  │  ① Pydantic 校验：query 1~300 字、top_k 1~10、空白拒绝
  │  ② Bearer Token（可选）+ SQLite 每 IP/每日/全站额度 + worker 信号量，超限 429
  │  ③ 创建脱敏 trace_id（存查询哈希和长度，不存问题正文）
  ▼
ask() 路由漏斗（rag_ask.py）
  0. 枚举查询（"有哪些/列举"）→ 知识库分类内过滤枚举 →（可选）LLM 分组整理
  1. 配方/设备意图或纯名称 → recipes.json 结构化直查（歧义 → 候选列表；设备 → 配方卡）
  2. 其余 → 一次语义规划（LLM 只输出计划：问题类型/实体/关键词/子查询/白名单 routes）
     ├─ 解释性关系（"喜欢/性格"）→ 原文证据窗口（不写图）
     ├─ 明确关系/多跳 → 图谱路径 + 证据 → 图未命中回退 RAG
     └─ 开放问题 → 多路检索（RAG + 子查询 + 全文关键词 + mention）→ 合并去重
  3. （可选）LLM 基于检索片段生成带 [来源N] 引用的回答；低相关 → 诚实拒答
  4. 写入阶段耗时、路由、检索排名、引用、模型/token 用量与版本；响应体和 X-Trace-ID 返回 trace_id
```

Trace 是旁路能力：创建或落盘失败时直接降级为无 Trace 问答，不允许可观测性故障拖垮主请求。
`llm_client.py` 通过请求上下文 observer 透出 usage，但 `chat()` / `chat_json()` 的返回类型保持不变，
因此旧调用和离线测试不需要理解 Trace。

### 3.3 线上质量闭环（从“答错了”到“以后不再错”）

```text
固定基线：数据集 hash + 索引 hash + Git commit + 检索参数 + Prompt 版本
      │
线上问答 ──► 脱敏 Trace（阶段/路由/排名/耗时/usage，不存正文）
      │
用户主动点“有用/没用” ──► feedback 隔离区（此时才保存问题与当时答案）
      │
管理员审核 ──► 无效样本驳回 / 补必要事实、可接受来源、正确路线或应拒答 Gold
      │
approved_regression ──► retrieval / pipeline / answer Replay
      │
候选结果 + 建议归因 ──► 人工确认 ──► 修代码/数据/索引 ──► 固定集防回归
```

这条链路是本项目“Agent 会越来越可靠”的工程基础，但它**不是让 Agent 自动学习线上输入**：

- `rag_config.py` 集中 embedding/检索参数，`rag_prompts.py` 集中实际 Prompt 并按内容自动生成版本哈希；
  `build_eval_manifest.py` 固化可跨机器复现的离线比较条件；实际 LLM/Git 状态写入每次评测元数据和 Trace，
  不把本机 `.env` 模型或无法解析的占位符混进静态清单；
- 普通 Trace 只保存查询 SHA-256、长度和脱敏阶段元数据。语义计划中的实体、关键词、改写文本也不原样落库，
  只保留数量、白名单路由和查询哈希；
- `POST /api/feedback` 受独立额度/可选令牌保护，必须提交真实 `trace_id`、匹配的 query hash、客户端和
  后端回答快照；同一 Trace 的相同提交幂等，冲突提交拒绝；
- 所有反馈先进入 `pending_review`，**不会自动进入评测集、索引、Prompt 或质量门禁**；
- 管理员批准样本时必须明确 `expected_route`；非拒答样本还要补必要事实或可接受来源，防止 Gold 残缺；
- 固定评测和 Replay 共用 `eval_case.py` 的 Gold schema 与确定性评分，避免两套口径漂移；
- Replay 默认 `retrieval` 是保留生产直取兜底的纯检索探针，不伪造 `route_used`；`pipeline` 和 `answer`
  都可能调用语义规划 LLM，必须显式添加 `--allow-llm`；
- 归因按 retrieval → pipeline → answer 瀑布执行，上一层失败就停止，图缺边/实体单列 `GRAPH_MISSING`；
- 自动归因只给建议（如 `ROUTE_WRONG`、`RECALL_MISS`、`FUSION_DROP`、`GRAPH_MISSING`、
  `GENERATION_INCOMPLETE`），最终结论必须人工确认。

**为什么不直接把点踩样本拿去训练/调参**：用户反馈可能是误触、主观不满、恶意输入或过期事实；
如果未经隔离和 Gold 审核就进入门禁，系统会把噪声当目标，甚至让线上输入反向污染正式知识库。
所以这套闭环追求的是“可复现地发现和修复失败”，不是“无人审核的在线自学习”。

---

## 4. Agent Loop 架构与设计理由（重点）

### 4.1 分层意图识别：规则优先、LLM 兜底

`intent_router.py` 是三层漏斗，**从便宜到贵**：

```text
L1 规则层：关键词/正则，毫秒级零成本、零外部依赖（配方/设备/知识/比较/数值）
L3 LLM 兜底：仅当规则未命中且配置了 key 才调用 chat_json（conf=0.9）
（L2 轻量分类器：评估后未启用——规则+LLM 已够用，不加没有收益的层）
```

**设计理由**：
- 80% 的查询（"怎么合成/是什么/哪个好"）有强正则特征，规则层直接命中，不花钱、不延迟；
- 规则层只赚不亏：它命中时置信度给 1.0，误判由后续路由漏斗纠正（§4.2 的"语义计划纠正宽规则"）；
- `classify_batch` 把多条未命中查询合并成一次 LLM 调用，批量省额度。

### 4.2 路由漏斗：结构化优先，LLM 最后

**能用配方表、分类或图边确定的内容，绝不让 LLM 猜。** 路由顺序是成本/确定性由高到低：

1. 枚举 → 分类内过滤（纯数据操作）；
2. 配方/设备 → recipes.json 直查（含歧义候选、设备产物反查）；
3. 实体明确 → 直接取整条全文当上下文（绕开 chunk 切分丢表格）；
4. 明确关系/多跳 → 图谱路径（带证据）；
5. 剩余开放问题 → 多路检索 + LLM 生成。

**设计理由**：
- 结构化直查 100% 准确、可复现、零 LLM 成本——RAG 是对"无法结构化"内容的补充，不是替代；
- 路由用**规则抢答 + 语义计划纠正**：`ask()` 先跑零成本规则抢占能直查的路径，直查未命中后，
  用语义计划的问题类型纠正宽规则的误判（如"重息壤是什么"被规则归为"知识"，但配方直查成功 → 直接返回）。
- 每个失败路径都有**明确回退**（图未命中 → RAG；直查未命中 → 检索），保证"失败可见"而不是静默出错。

### 4.3 语义规划器：planner 与 generator 分离（防幻觉注入）

`semantic_plan()` 用**一次**受约束 LLM 调用输出检索计划：

```json
{"question_type":"preference","topic":"饮食偏好","entities":["人物名"],
 "keywords":["吃","食物","饮食","喜欢"],
 "search_queries":["人物名 吃 饮食偏好"],
 "routes":["entity_direct","rag","keyword"],"needs_graph":false}
```

**设计理由（这是全系统最重要的防幻觉设计）**：

- **规划器不能回答问题**，也不能把猜测的答案写进关键词。系统提示明确写着
  "你是检索规划器，只输出 JSON；计划中的词不是事实证据"。最终结论必须来自结构化数据、原文或 RAG 命中；
- 计划字段全部经过白名单清洗（`question_type` 只能取 9 种枚举、`routes` 只能取 8 种白名单、
  实体名必须存在于知识库、列表长度/字数受限）——**LLM 的输出永远不能直接变成执行动作**；
- 一次规划同时承担了"意图理解 + 查询改写"，替代原来两次独立的 LLM 调用（低置信意图兜底 + 改写），
  把开放问题压到 2 次 LLM；
- 规划失败时返回**确定性 fallback 计划**（含地名 → "地名 解锁/开放条件/前置任务" 子查询），
  系统不因 LLM 抖动而崩溃（§6 降级）。

**踩坑案例**（§8.3）：早期规划器把"喜欢吃什么"抽象成"人物关系"（因为"喜欢"二字），
导致偏好问题被误送进关系路线。修复：规划提示词要求**保留决定主题的核心动词**（吃/穿/用/去），
并把"喜欢吃什么"这类识别为 preference 走原文检索。

### 4.4 图检索与文本检索协作（轻量 GraphRAG）

```text
关系问题 → 图检索 → 路径 + 原文证据 → LLM 整理
图未命中 → 文本 RAG 回退（"图谱无证据" ≠ "关系不存在"）
主观问题 → 原文证据窗口（不把模型判断写成事实边）
```

**设计理由**：
- RAG 擅长"找原文"，图谱擅长"找明确关系和路径"；两者互补，图谱不是 RAG 的替代品；
- 关系边**只从结构化字段、配方结构或原文明确句式建立**（白名单规则，`extraction_method` 记录来源），
  **不允许 LLM 把猜测直接写入正式图**；同场出现只作为检索线索，不自动等价为事实关系；
- 每条边保存来源、原文证据（500 字以内）、置信度、提取方法与审查状态 → **每条事实可追溯、可审计**；
- 图路径最多 3 跳（防路径爆炸），超限回退文本，不解释为关系不存在；
- 别名只保存审定过的映射，不把模糊相似词自动当别名。

### 4.5 事实与解释分离（复合问题的正确姿势）

`GEN_SYSTEM` 强制 LLM 对解释性问题分三段回答：**原文明确事实 / 基于证据的合理解读 / 资料不足**，
解读不得伪装成设定事实；复合问题必须逐项回答，某一小问资料不足只说明该小问，不能拒绝整道题。

**设计理由（踩坑驱动）**：
- 案例"狼卫的妹妹是谁，她可爱吗"：亲属关系是事实（图谱边负责），"可爱"是主观（原文证据负责）；
  旧版因为主观证据不足会**连带拒绝已确定的姓名**——这是错误的。修复：事实与解释走两条通道，
  子问独立评估（§8.2）。
- "喜欢/中意/信任/性格"等解释性词不固化成图谱边，用 `relationship_evidence_hits()` 从任务、档案、
  对话原文中抽局部证据窗口，生成时强制区分事实/解读/不足。

### 4.6 长文档上下文聚焦（截断预算审计）

旧版 `gen_answer()` 每来源固定取开头 1500 字 → 答案在档案后半段时**永远不可见**。
现用 `focus_long_context()`：jieba + 实体名 + 关系/评价词提取查询词 → 全文按句切分 →
先为每个查询词保留最佳证据窗口（防复合问题只覆盖一个子问）→ 再按分数补足 →
每来源 1800 字预算、最多 7 个窗口、窗口间有省略分隔。

**设计理由**：LLM 上下文有硬预算，但**截断必须是查询驱动的，不能是位置驱动的**。
每来源 1800 字 / 7 窗口 / top-k=5 / 图 3 跳 / 枚举 40 项 / 前端摘要 260 字，全部是显式预算并
记录在 `KNOWLEDGE_SYSTEM_ARCHITECTURE.md §6`——任何一项被改都有回归测试守着。

**踩坑案例**：枚举超过 40 项时，模型必须拿到**真实总数**并声明"只展示前 40 项"——
防止 LLM 把截断清单说成完整清单（§8.2）。

### 4.7 为什么不引入开放式 Agent Loop（以及受限循环的上线门槛）

这是本项目**最克制、也最值得迁移**的决策。当前没有通用工具协议、观察后重新规划、工作记忆或开放循环。

**理由**：
1. 开放式 Loop 不会比脚本直查更快：每轮串行网络调用增加延迟；
2. 不可复现路径增加：同一问题两次问可能走不同工具序列，评测和排障变难；
3. 错误面扩大：工具越多，选错工具/格式错误的概率越大；
4. 本系统的确定性工具（配方表、枚举、图边）本来就不需要模型反复选择——**确定性工具配确定性调度**。

**保留的受限循环形态**（只有困难评测证明"第一轮证据不完整"是主要剩余错误才启用）：

```text
检索 → 审查子问题覆盖率
  ├─ 足够：生成
  └─ 不足：LLM 从白名单选择一次补检索 → 再审查 → 生成或拒答
```

约束清单（上线门槛，写在 `KNOWLEDGE_SYSTEM_ARCHITECTURE.md §5.1`）：
最多一次补检索、JSON 参数校验、总超时、完整 trace、不写长期记忆、不允许模型修改图谱；
白名单仅含 graph / RAG / entity direct / mention-keyword / recipe / 枚举。
**上线门槛**：困难集端到端正确率显著提升且拒答正确率不下降，否则保持现状。

### 4.8 诚实拒答（答案边界）

- 普通检索 top-1 `vector_sim < 0.30` → 返回"知识库中未找到足够相关的资料"，**不硬编**；
- direct / mention / keyword / relationship_evidence 命中是**人工确认的相关上下文**，
  不被向量阈值误拒（`has_curated` 判断）——避免"明明有证据却被阈值拒掉"；
- LLM 未配置或调用失败时安全降级（§6）。

---

## 5. 安全审查设计（从里到外）

### 5.1 凭据与密钥

| 措施 | 实现 |
|---|---|
| 密钥不进代码 | `llm_client.py` 零明文 key，只从环境变量 / `.env` 读取 |
| 密钥不进仓库 | `.env` / `*.key` / `*.pem` 在 `.gitignore`；提供 `.env.example` 模板 |
| 密钥不进镜像 | Dockerfile 不 COPY `.env`；运行时用 `--env-file .env` / compose `env_file` 注入 |
| 错误信息脱敏 | `_safe_body()` 把响应体里的 key 替换为 `***`；`config_summary()` 只报 `key_configured` 布尔 |
| 不在日志/截图出现 | 部署手册明确"不要在聊天、截图或日志中展示 .env 内容" |
| 权限 | 服务器 `.env` `chmod 600`，只允许所有者读写 |

**设计理由**：网页前端无法安全保存"前端 API 密钥"（任何人打开 DevTools 都能看到），
所以费用保护只能靠**限流 + 并发上限 + 服务商预算报警**三层，而不是藏 key。

### 5.2 应用层输入边界与并发

- `AskRequest`：`query` 1~300 字（strip 后拒绝空白）、`top_k` 1~10（Pydantic `ge/le`）；
- `/api/synthesis`：空 item 拒绝、负 max_depth 拒绝；
- `/api/ask` 可配置 Bearer Token；未配置时仍不是无限匿名服务，SQLite 会以事务方式执行
  每 IP 每分钟、每 IP 每日和全站每日额度，多个 worker 共享同一计数库；计数库异常时付费问答 fail closed；
- 每 worker 用 `threading.BoundedSemaphore(ASK_MAX_CONCURRENCY=2)` 限制同时执行的付费问答，
  满则返回 429 + `Retry-After: 3`（**进程内并发上限，防止慢 LLM 请求耗尽线程和额度**）；
- `/api/feedback` 不凭空接受问题：独立限流并复用可选 Bearer Token；必须绑定已有随机 `trace_id`，
  query hash、客户端和后端回答快照均须一致；相同重试幂等、冲突提交拒绝，避免直接向隔离区灌入文本；
- worker 数默认 1（`WEB_CONCURRENCY` 环境变量控制），避免小内存服务器重复加载模型。

### 5.3 网络与容器安全

| 层 | 措施 |
|---|---|
| 防火墙 | 公网只开放 80/443，**不开放 8000** |
| Nginx | `/api/ask` 单 IP 平均 6 次/分钟、burst 2、并发 1，超限返回 JSON 429 + `Retry-After` |
| 应用 | `/api/health/deep`、`/api/metrics` 仅允许回环客户端或有效管理令牌；不信任未配置代理来源的转发头 |
| Nginx | `server_tokens off`、`client_max_body_size 16k`、nosniff/SAMEORIGIN/Referrer-Policy 头 |
| Nginx | 代理模式（Cloudflare）需按官方网段配置 `real_ip`，**不无条件信任 X-Forwarded-For** |
| Compose | 只绑 `127.0.0.1:8000`、安全计数/Trace SQLite 持久卷、`cap_drop: ALL`、`no-new-privileges`、`pids_limit: 256`、日志轮转 10m×3 |
| 容器 | 健康检查（`/api/health`）、`restart: unless-stopped`、`init: true` |

**设计理由**：知识问答调用付费 LLM，必须把"谁能触发付费"和"触发频率"都管住。
Nginx 处理边缘突发，应用 SQLite 处理跨 worker 的分钟/每日总额度，信号量限制当前进程资源占用；
三层解决的问题不同，任何一层都不能替代另外两层。

### 5.4 内容安全

- **媒体代理白名单**（`/api/media`）：仅 `https` + 主机名必须 `bbs.hycdn.cn` + 路径必须以
  `/image/` 或 `/audio/` 开头 + **禁止跟随重定向** + 流式读取硬限制 ≤25MB + 拒绝压缩编码 +
  上游 content-type 必须是 image/audio + 下载/发送并发有界 ——
  这是**SSRF 的最小化实践**：代理必须明确"允许代理什么"，而不是"禁止代理什么"；
- **图谱写入白名单**：LLM 永远不能写正式图；只有规则构建器能加边，且每条边带来源与证据；
- **规划器输出白名单**：LLM 计划字段枚举校验（§4.3）；
- **生成边界**：LLM 只能根据检索资料回答 + `[来源N]` 引用；低相关拒答。

### 5.5 数据真实性纪律

- 原始 WIKI JSON 只读；解析不到的内容**如实报告，不补写猜测数据**；
- 抓取、提取失败如实上报（"抓不到如实报告，禁止编造"是项目级纪律，见 `AGENTS.md`）；
- 评测数字是"固定规则和固定数据上的回归结果"，文档明确"不应表述为所有自然语言问题 100% 正确"。

### 5.6 Trace 与用户反馈隐私

| 数据 | 默认保存 | 明确不保存 / 约束 |
|---|---|---|
| 普通 Trace | query SHA-256、长度、客户端类型、路由、阶段耗时、脱敏检索引用、usage、版本 | 不保存问题/答案正文；不原样保存实体、关键词和改写查询 |
| 主动反馈 | 问题、可选说明、当时显示的答案、vote、关联 trace_id | 仅用户点击后写入；先隔离，不自动训练或进入门禁 |
| Replay 报告 | 已批准样本的 baseline/candidate 与建议归因 | 写入 Git 忽略目录；答案模式需显式允许在线调用 |

Trace SQLite 和 Replay 报告可能含用户主动提交的正文，所以它们属于运行数据而不是源码：默认位于
`logs/` 或 `output/eval/replay/`（均不进 Git），生产环境放受限持久卷并设置访问权限与保留周期。
可观测性的目标是定位失败，不是尽可能多地收集用户内容。

---

## 6. 降级方案（逐层，任何一层挂了都不崩）

| 触发条件 | 降级行为 | 保证 |
|---|---|---|
| 未配置 `LLM_API_KEY` | `llm.available()` 返回 False；结构化直查/检索结果照常返回；`gen_answer` 跳过 | 配方树、设备卡、知识库回退 100% 可用 |
| LLM 调用失败/超时 | `except Exception` 捕获；保留检索结果；`llm_degraded` 计数（`rag_monitor`） | 请求不 500 |
| 语义规划失败 | 返回确定性 fallback 计划（地名→"解锁/开放条件/前置任务"子查询） | 开放问题仍可检索 |
| 图不可用/未命中 | `graph_result = {"available": False}` → 回退多路 RAG | "图无路径"≠"关系不存在" |
| 检索低相关 | 诚实拒答（"知识库中未找到足够相关的资料"） | 不硬编 |
| 问答并发满 | HTTP 429 + Retry-After | 不排队打爆 LLM 额度 |
| 媒体上游失败 | HTTP 502；前端显示明确加载失败状态 | 页面不白屏 |
| 索引/BM25 分片损坏 | `build_rag --incremental` 自愈（缺失/陈旧/损坏/多余分片自动重建） | 索引可恢复 |
| Trace 创建/写入失败 | 捕获异常，继续执行原问答；响应不带 trace_id | 可观测性故障不扩大为业务故障 |
| token usage 缺失 | 该调用仍记录模型、阶段、耗时和状态，token 字段为空 | 兼容不返回 usage 的 OpenAI 兼容服务 |
| 容器崩溃 | `restart: unless-stopped` + 健康检查 | 自动拉起 |
| 新版本失败 | git 回滚到上一提交重新构建（§7.4） | 版本可恢复 |

**设计原则**：**降级路径必须和主路径一样被测试**。`tests/test_api_server.py` 有
`test_ask_endpoint_delegates_without_network`、`test_blank_query_is_rejected` 等用例；
`rag_monitor` 专门统计 `llm_degraded` / `empty_retrieval` / `rejected` 速率——降级不是"侥幸不崩"，
而是**被观测、被计数、被门禁**的行为。

---

## 7. 版本恢复与数据安全

### 7.1 数据分层：事实源 vs 可重建产物

| 层 | 内容 | 是否进 Git | 丢失后 |
|---|---|---|---|
| 事实源（只读） | WIKI 原始 JSON | ❌（大文件，可重新采集） | 重新采集 |
| 规范产物 | `endfield_kb/` | ✅ | 由 build_kb_all 重建 |
| 派生产物 | `output/rag/`（含 Chroma）、`graph.db` | ❌（可重建） | Dockerfile 构建时自动重建 |
| 评测基线 | 固定评测集、`eval_manifest.json`、已审定结果 | ✅ | 用于复现和比较版本，不含线上用户正文 |
| 运行状态 | 请求额度 SQLite、Trace/feedback SQLite、Replay 报告 | ❌ | 独立持久化；可能含用户主动反馈，不能随源码公开 |
| 模型缓存 | `BAAI/bge-small-zh-v1.5` | ❌（体积大） | 构建阶段下载 |

**设计理由**：把"可重建的东西"排除出版本控制，仓库保持轻量且可复现；
`Dockerfile` 在构建阶段下载模型 → 切离线 → 全量重建索引 → 运行阶段完全离线。
**密钥永远不进入镜像**（`.env` 只通过运行环境注入）。

### 7.2 增量更新与一致性校验（数据正确性的核心机制）

- `record_content_hash()` = md5( schema 版本 + name + category + full_text + sections )；
  **索引策略版本（`INDEX_SCHEMA_VERSION`）参与哈希**——"代码改了但原文没变"也会触发重建，
  这是防止"切分算法升级后增量构建永远不感知"的关键（§8.1 踩坑）；
- `chunks.json` manifest 是审计基线：增量对比新旧 hash → 只 upsert 变更 chunk / delete 删除 chunk；
- Chroma 按 1000 条分批 upsert/delete（规避批量上限）；
- BM25 按分类分片，增量只重建变更分类，并用 `inconsistent_bm25_categories()` 自愈；
- `rag_audit.py` 核对 KB / manifest / Chroma / BM25 四方一致，`/api/health/deep` 暴露给运维。

### 7.3 图谱增量与幽灵关系

`build_knowledge_graph.py --incremental`：
来源内容 hash 对比 → 对每个变更/删除来源，**先 DELETE 该来源的旧边/派生别名，再重建** →
manifest 同步更新；同时清理 recipe_rule 边与无引用的合成实体。

**设计理由（踩坑驱动）**：如果只增不删，来源修改后旧关系会残留成"幽灵关系"——
图谱会回答一个已经不存在的事实。先删后建保证**关系集合与来源一一对应**。
错误修复原则：**修规则后按来源重建，绝不手改一条数据库记录**（手改不可复现、不可审计）。

### 7.4 部署回滚

```bash
git rev-parse HEAD                  # ① 更新前记录当前提交
git pull --ff-only origin master    # ② 更新（--ff-only 防止服务器生成合并提交）
docker compose build && docker compose up -d
# 出问题 →
git switch --detach <previous_commit>
docker compose build && docker compose up -d
git switch master                   # 确认恢复后再回主分支
```

- 回滚**不会删除 `.env`**，也不需要删 Docker 数据目录——密钥与数据独立于代码版本；
- 数据与索引都构建在镜像内（无挂载目录），所以回滚 = 重建镜像 = 回到当时的数据快照；
- 文档明确"不要通过删除 `/var/lib/docker`、项目目录或 `.env` 来回滚"（防止手滑扩大故障）。

### 7.5 部署中的密钥流转

```text
本机 .env（gitignore）→ GitHub（无密钥）→ 服务器 cp .env.example .env + chmod 600 → compose env_file 注入
Railway：Variables 设置，不上传 .env，不把 Key 写入 Dockerfile
小程序：apiBase 指向 HTTPS 域名，Key 只放后端，绝不写进小程序代码
```

### 7.6 评测版本与坏例资产的恢复边界

`eval_manifest.json` 把四类固定数据集 hash、索引 manifest hash、embedding 模型、按内容自动生成的
Prompt 版本以及 BM25/向量/RRF/top-k 参数绑成可跨 Windows/Linux 比较的静态基线；文本哈希统一换行。
实际 Git commit、工作区 dirty 状态和 LLM 模型由每次评测结果与 Trace 实时记录。
它解决的是“这两个分数是否在同一条件下产生”，不是替代实际评测结果。

线上 feedback 数据库不随 Git 回滚：代码回到旧版本后，历史 Trace 与反馈仍保留在持久卷中；
Replay 报告按时间戳新建、不覆盖旧运行。这样代码版本、索引版本、当时回答和候选回答能同时追溯，
也避免为了回滚应用而误删坏例证据。若必须清理用户数据，应按明确的反馈 ID/保留期执行，而不是删除整个项目目录。

---

## 8. 踩过的坑与重要错误（按主题归位）

> 每条格式：**现象 → 根因 → 修复 → 现在怎么防**。完整决策链见 `RAG_DEVLOG.md`。

### 8.1 数据与索引

| # | 坑 | 根因 | 修复与防线 |
|---|---|---|---|
| 1 | 53 个长条目产生 **0 chunk**（丢 370 chunk） | `sections` 为空时切片逻辑直接跳过 | `split_chunks` 回退切 `full_text`；测试 `test_long_record_without_sections_falls_back_to_full_text` |
| 2 | 莱万汀冰淇淋档案**静默漏切片** | `sections` 只覆盖部分正文，档案后半段/攻略段没进索引 | `uncovered_full_text()` 把未覆盖的 `full_text` 作为"全文补充"分块；测试覆盖 |
| 3 | 切分代码升级后**增量构建不感知** | hash 只含原文，不含索引策略 | `INDEX_SCHEMA_VERSION` 进 hash（schema + 正文 + sections 共同指纹） |
| 4 | Chroma 全量写入**批量上限失败** | 单次 upsert 超 SQLite 批量限制 | 按 1000 条分批；manifest 全量重写为基线 |
| 5 | "重息壤/向心之引"被 jieba **切碎 → BM25 失配** | 通用词典不认识游戏专名 | `gen_jieba_dict.py` 扫描名称生成 `dict_zh.txt`，构建/检索加载同一词典 |
| 6 | 检索 Recall@5 只有 **72%** | 短条目名称不参与召回 | chunk 加"分类+名称"前缀 → 85%；再加名称检索与评测字符归一化 → 100% |
| 7 | BM25 分片**陈旧/损坏/多余** | 增量重建分类集合不完整 | `inconsistent_bm25_categories()` 对比 manifest 检测并自动重建 |

### 8.2 检索与生成

| # | 坑 | 根因 | 修复与防线 |
|---|---|---|---|
| 8 | 长条目答案**固定截开头 1500 字不可见** | 位置驱动截断 | `focus_long_context()` 查询驱动分散选段；回归测试覆盖"答案在 1500 字后" |
| 9 | "狼卫的妹妹是谁，她可爱吗"**整题被拒** | 子问题证据不足连坐整题 | 事实（图边）与主观（原文窗口）分离；`GEN_SYSTEM` 要求逐项回答、子问独立说明不足 |
| 10 | 枚举 40+ 项时模型**把截断清单说成完整清单** | 模型不知道真实总数 | prompt 给真实总数 + 强制声明"仅展示前 N 项"；`test_large_enum_prompt_discloses_total_and_truncation` |
| 11 | 检索 top-1 相似度低但**有确定性命中**却被阈值误拒 | 阈值一刀切 | direct/mention/keyword/evidence 命中视为人工确认上下文，豁免向量阈值 |
| 12 | **宽规则误判**（配方问题被判成知识） | 正则过于宽泛 | 直查抢答 + 语义计划纠正：直查成功直接返回，未命中才用计划类型覆盖 |

### 8.3 图谱与关系

| # | 坑 | 根因 | 修复与防线 |
|---|---|---|---|
| 13 | "喜欢吃什么"被**误送人物关系路线** | 规划器看到"喜欢"就归类关系 | 规划提示词要求保留核心动词（吃/穿/用/去）；`is_interpretive_relation` 要求第二人物/亲属线索才走关系证据 |
| 14 | 同场出现被当成**事实关系** | 共现 ≠ 关系 | 白名单规则 + 证据字段；同现只作检索线索 |
| 15 | 来源修改后旧关系**残留（幽灵关系）** | 只增不删 | 增量先删来源旧边再重建；`graph_audit` 校验来源 hash 一致性 |
| 16 | "武陵的领袖"被**扩大成政治职务** | 关系词过译 | 回答保留原文"管代/管理负责人"含义，不添加无证据的职务描述 |
| 17 | 别名误收录（模糊相似词当别名） | 别名来源过宽 | 只存审定别名；`human_verified` 别名增量删除时受保护 |

### 8.4 环境与工程（Windows 特有）

| # | 坑 | 根因 | 修复与防线 |
|---|---|---|---|
| 18 | 终端中文 **GBK 乱码** | Windows 默认编码 | 所有中文/结果写 UTF-8 文件再读取；`sys.stdout.reconfigure` 前判断 `if sys.stdout:`（后台运行时可能为 None） |
| 19 | 模型加载**联网检查 HF 超时/失败** | 默认会访问网络 | `HF_HUB_OFFLINE=1` + `local_files_only=True` 强制离线 |
| 20 | 浏览器 `localhost` 打不开，`127.0.0.1` 可以 | Windows localhost 解析到 IPv6，uvicorn 绑 0.0.0.0 只听 IPv4 | 文档/启动说明统一用 `http://127.0.0.1:8000` |
| 21 | 全量重建时 **uvicorn 锁住 Chroma 文件** | 运行中占用 | 重建前先停服务；CI 与部署文档写明顺序 |
| 22 | 后台进程 stdout 为 None 导致崩溃 | Windows 服务/无控制台环境 | `if sys.stdout:` 容错（AGENTS.md 纪律） |

### 8.5 部署与运维

| # | 坑 | 根因 | 修复与防线 |
|---|---|---|---|
| 23 | Docker 镜像**数 GB 的 CUDA 依赖** | sentence-transformers 在 Linux 解析出 CUDA torch | 先从 PyTorch 官方 CPU 仓库锁定 `torch==2.x+cpu` 再装依赖 |
| 24 | 模型下载与离线顺序颠倒导致构建失败 | 先切离线再下载 | 构建分两步：先下载模型入缓存 → 再切 `HF_HUB_OFFLINE=1` → 重建索引 |
| 25 | Cloudflare 代理下**按 IP 限流误伤所有用户** | Nginx 看到代理节点 IP | 首发用"仅 DNS"模式；开代理需按官方网段配 `real_ip`，不无条件信任 XFF |
| 26 | Certbot **自动改写线上 Nginx 配置**后模板覆盖冲突 | 手工模板与证书配置不兼容 | 修改模板时人工合并，先 `nginx -t` 再 reload；不直接覆盖 Certbot 已写的配置 |
| 27 | 多 worker 小内存服务器 **OOM** | 每个 worker 各加载一份模型+索引 | 默认 `WEB_CONCURRENCY=1`，确认内存余量后再逐步提高 |
| 28 | 部署密钥泄漏（截图/日志/聊天） | 人为疏忽 | 手册明令禁止展示 `.env`；权限 600；`.env` 不入 Git |

### 8.6 评测、Trace 与坏例闭环

| # | 坑 | 根因 | 修复与防线 |
|---|---|---|---|
| 29 | Recall@5=100% 被误解为“检索已解决” | 71 条集偏小且生成时存在 core 子串约束；同时 Precision@5 约 0.42 | 把它定义为开发回归集而非真实用户分布；后续另建 holdout/challenge/hard negatives |
| 30 | 只看本次分数，无法解释为什么变化 | 数据、索引、Prompt、代码或参数可能同时变化 | `eval_manifest.json` 固化各层 hash、版本和检索参数；dirty 状态明确记录 |
| 31 | Trace 为排障方便而意外保存用户语义文本 | semantic plan 内含实体、关键词和改写查询 | 普通 Trace 只存原查询/子查询哈希；计划只留类型、路由、计数和哈希，正文只在主动反馈后保存 |
| 32 | 可观测性数据库故障导致主问答失败 | Trace 被放进业务主路径且异常外溢 | HTTP 包装层 fail open：Trace 创建/finish 均独立捕获；单测模拟不可写数据库 |
| 33 | 点踩直接进入回归集会污染 Gold | 误触、恶意反馈、主观偏好和事实错误混在一起 | `pending_review` 隔离；批准时必须补事实/来源/路线/拒答标签，Replay 只读取批准样本 |
| 34 | Replay 结果覆盖旧运行或自动归因被当成结论 | 缺少时间/版本边界，规则归因只能看到部分证据 | 报告按 UTC 时间戳新建，记录 commit/mode/trace；归因标记 `human_confirmation_required` |

---

## 9. 可迁移的思路或设计

> 以下每一项都不绑定本项目，可搬到其他 RAG / Agent / 部署项目。每项给"是什么 → 适用场景 → 迁移注意"。

### 9.1 架构级

| 思路 | 是什么 | 适用场景 | 迁移注意 |
|---|---|---|---|
| **确定性优先，LLM 最后** | 能用表/分类/规则确定的路径绝不交给模型 | 任何有结构化数据兜底的问答系统 | 先盘点你有哪些"确定性能回答"的数据，再谈 RAG |
| **规则漏斗 + 计划纠正** | 便宜规则抢答，语义计划纠正宽规则误判 | 意图分类不准但"能直查"的领域 | 纠正逻辑要能证明直查真的命中了，防止二次误判 |
| **planner / generator 分离** | 规划 LLM 只产计划，不产事实；计划字段白名单校验 | 一切"LLM 决定检索策略"的设计 | 计划 schema 必须枚举白名单 + 长度限制，LLM 输出不能直接变动作 |
| **受限 Agent Loop 门槛** | 不默认引入开放式 Loop；只有评测证明"第一轮证据不全"是主要错误才加，最多补一次 | 任何考虑 Agent 化的 RAG 项目 | 写清上线门槛（正确率提升 + 拒答不降），没有收益不加复杂度 |
| **事实与解释分离** | 事实（图/表）与主观（原文证据）分通道，子问独立评估 | 人物关系/评价类问答 | 复合问题逐项评估，不能一票否决整题 |
| **诚实拒答** | 低相关不硬编，输出"资料中未找到" | 一切面向事实的 LLM 应用 | 拒答阈值要配合"人工确认命中豁免"，否则会误杀有证据的答案 |
| **离线执行链 + 线上改进链分离** | 问答管线只负责稳定回答；反馈/审核/Replay 在旁路闭环进行 | 专精 RAG、非在线学习系统 | 不要让线上反馈直接修改 Prompt、索引、图谱或长期记忆 |

### 9.2 数据工程级

| 思路 | 是什么 | 适用场景 | 迁移注意 |
|---|---|---|---|
| **内容 hash + 索引策略版本** | 指纹 = 数据 + 代码版本，增量构建只重建变更 | 任何可重建索引的增量更新 | 代码/切分策略变化时必须 bump 版本，否则增量永不感知 |
| **可重建产物不入库** | 派生数据排除出 Git，构建时重建 | 索引/向量库/模型缓存 | 要有确定的重建脚本（Dockerfile/CI），否则"可重建"变成"不可重建" |
| **先删后建防幽灵数据** | 增量时先删来源旧产物再重建 | 图谱、派生表、聚合缓存 | 删除范围要和来源 ID 严格绑定，防止误删他源数据 |
| **索引自愈** | 审计检测缺失/陈旧/损坏分片并自动重建 | 分片式索引 | 自愈逻辑本身要幂等、可审计，并在测试里覆盖 |
| **审计与 manifest** | 每份产物有机器可读基线（chunks.json / build_status.json） | 一切需要"坏了能说清坏在哪"的数据管道 | manifest 是审计基线不是唯一真相，要与实际产物交叉核对 |
| **版本矩阵而非单个 run id** | 一次评测同时绑定代码、数据集、索引、Prompt、模型和参数 | 所有需要比较前后效果的 AI 系统 | run id 只标识一次运行；没有输入版本矩阵，两个 run 仍不可公平比较 |
| **运行状态与源码分层** | Trace/反馈是持久运行数据，评测基线是可提交审计资产 | 含用户反馈的应用 | 用户正文库和 Replay 报告不进 Git；基线 manifest 可进 Git但不得含正文 |

### 9.3 安全与运维级

| 思路 | 是什么 | 适用场景 | 迁移注意 |
|---|---|---|---|
| **代理白名单而非黑名单** | 媒体代理只允许特定域名+路径+类型+大小 | SSRF 高发场景（图片/文件代理） | 白名单要够窄（scheme+host+path+content-type+size） |
| **降级可观测** | 每种降级都有计数器（llm_degraded/empty/rejected）和门禁 | 依赖外部 LLM 的服务 | 降级路径要和主路径一样有测试，不能"侥幸不崩" |
| **三层费用保护** | Nginx 边缘限流 + 应用事务式配额 + 进程并发上限 | 付费 LLM 的公开服务 | 分别处理突发流量、跨 worker 总额度和资源占用；计数库故障应 fail closed |
| **镜像内数据自包含** | 索引/模型构建进镜像，运行离线 | 需要离线运行的部署 | 构建要可复现（锁版本、固定源），密钥绝不进镜像 |
| **git 提交回滚 + .env 独立** | 回滚只动代码版本，密钥文件独立存在 | 任何自托管部署 | 记录更新前 commit；`--ff-only` 防合并提交；回滚不删数据目录 |
| **真实 IP 处理** | 代理/CDN 场景按官方网段配 real_ip | 部署在 Cloudflare 等代理后面 | 不无条件信任 X-Forwarded-For |

### 9.4 验收与质量门禁级

| 思路 | 是什么 | 适用场景 | 迁移注意 |
|---|---|---|---|
| **分层门禁** | 索引/检索/路由/答案/运行/变更六层各自有指标与阈值（`quality_gate.py`） | 任何"改了怕回退"的系统 | 阈值是回归基线不是绝对正确率，文档要写明边界 |
| **离线单测 + CI 无密钥** | CI 只跑无网络测试与落盘指标，在线 LLM 评测由维护者显式刷新 | 避免 CI 产生模型费用/依赖密钥 | 评测产物落盘提交，CI 只做门禁不产生调用 |
| **前后端契约测试** | DOM id / 接口契约有测试守着（`test_frontend_contract.py`） | 单页应用频繁改 UI | 契约测试随 DOM 改动同步更新 |
| **开发集小而审慎** | 当前 71 条集适合回归，不包装成真实分布或最终 Gold | 一切自动生成评测集 | 自动生成 gold 要由结构化数据核对；另建人工双标 holdout/challenge，避免在开发集上反复调到 ceiling |
| **反馈先隔离再回归** | 点踩只产生候选坏例，人工补 Gold 后才允许 Replay | 有真实用户输入的 AI 应用 | 正例/负例都可能含噪声；审核状态和正式门禁必须物理/逻辑隔离 |
| **Replay 分层且默认零费用** | retrieval / pipeline / answer 分别定位召回、编排、生成 | RAG 坏例归因 | 默认跑到 pipeline；答案回放必须显式授权费用并保存候选版本 |
| **归因是建议，不是裁决** | 根据来源是否入库/入索引/入候选、路由和答案事实给失败类别 | 半自动排障 | 一类现象可能有多重根因；自动结果必须由人结合原始证据确认 |
| **Judge 也要版本和校准** | Judge Prompt 独立文件带版本，后续用人工双标校准 | LLM-as-a-Judge | 有序 0/1/2 分优先看加权 Kappa 与假通过率，不能机械套单一阈值 |

---

## 10. 相关文档与代码地图

| 文档 | 内容 |
|---|---|
| `KNOWLEDGE_SYSTEM_ARCHITECTURE.md` | 知识系统统一架构 + 路线图 + 门禁一体（现状与计划） |
| `RAG_DEVLOG.md` | 开发决策与踩坑记录（历史） |
| `output/GRAPHRAG_ARCHITECTURE.md` | 图谱专项细节 |
| `DEPLOYMENT.md` / `deploy/README.md` / `deploy/API_SECURITY.md` | 部署总纲、自有服务器手册、API 安全 |
| `web/README.md` / `miniprogram/README.md` | 前端设计与实现 / 小程序 |
| `scripts/README.md` | 全部工具与命令 |
| `tests/` | 离线回归测试（验收） |
| `.github/workflows/rag-quality.yml` | CI 质量门禁（验收自动化） |

| 代码 | 职责 |
|---|---|
| `scripts/rag_ask.py` | 问答主流程（本 Agent 的"大脑"） |
| `scripts/intent_router.py` | 分层意图识别 |
| `scripts/semantic_plan`（在 rag_ask.py 内） | 受约束语义规划器 |
| `scripts/llm_client.py` | LLM 统一客户端（密钥安全/降级/JSON 兜底） |
| `scripts/build_rag.py` | RAG 构建/增量/自愈 |
| `scripts/build_knowledge_graph.py` | 图谱构建/增量（防幽灵关系） |
| `scripts/graph_search.py` / `graph_audit.py` | 图检索 / 图审计 |
| `scripts/api_server.py` | FastAPI（输入边界/并发/媒体代理） |
| `scripts/rag_monitor.py` / `quality_gate.py` | 运行指标 / 质量门禁 |
| `scripts/build_eval_manifest.py` | 固化评测数据、索引、代码、参数和 Prompt 版本 |
| `scripts/rag_trace.py` | 脱敏 Trace、反馈隔离区 schema 与持久化 |
| `scripts/review_bad_cases.py` | 本地管理员审核反馈并补齐 Gold |
| `scripts/replay_bad_cases.py` | 批准坏例的分层回放与建议归因 |
| `scripts/prompts/judge/v1.txt` | 可版本化的答案 Judge Prompt |
