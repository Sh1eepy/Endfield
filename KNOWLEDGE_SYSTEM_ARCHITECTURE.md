# 知识库、RAG 与知识图谱架构（权威说明）

> 更新日期：2026-08-20。本文是知识问答系统的统一入口；实现细节以代码和评测产物为准。
> 配方合成树使用独立的结构化配方库，不应被改造成 RAG 推测。

## 1. 目标与优先级

本系统按“事实准确和证据可追溯 → 失败可见和稳定可复现 → 答案完整 → 延迟成本受控 → 表达自然”排序。
企业内部知识库通常也应如此。困难问题多花一次检索或模型调用是合理的，但额外步骤必须带来可测收益，
并有超时、次数上限、日志和降级路径。

## 2. 三套能力的边界

| 能力 | 数据 | 擅长 | 不擅长 |
|---|---|---|---|
| 结构化直查 | `recipes.json`、分类清单 | 配方、设备、确定性枚举 | 性格、剧情概括 |
| RAG | chunks、全文、BM25、向量、名称、mention | 描述、数值、档案、剧情、模糊问题 | 保证关系路径完整、穷举关系 |
| 知识图谱 | `knowledge_graph/graph.db` | 明确关系、正反问法、1～3 跳路径 | 未建模观点、长文本概括 |

知识图谱不是 RAG 的替代品。GraphRAG 是图检索和文本检索协作的系统形态；本项目实现轻量、可审计的
GraphRAG，不把所有文本都永久转成模型猜测的边。

## 3. 离线构建流程

```text
官方 WIKI 块式数据
  ├─ build_kb_all.py → 22 类 JSONL/Markdown（full_text、sections、来源标识）
  │    ├─ build_rag.py → contextual chunks → Chroma + 分类 BM25 + manifest
  │    ├─ mention_index → “谁的原文提到了实体 X”反查
  │    └─ build_knowledge_graph.py → 实体、关系、证据、来源哈希 → SQLite 图谱
  └─ recipe_extract.py → 真实配方结构 → recipes.json
```

### 3.1 RAG 切片规则

- 正常条目按 `sections` 切；长 section 按自然边界再分块，短内容合并，避免标题碎片。
- `sections` 为空但 `full_text` 有内容时回退切全文。此前 53 个长条目产生 0 chunk，现已修复。
- chunk 加 `【分类】名称：条目名` 前缀，使专名同时进入 BM25 和 embedding。
- jieba 加载项目专名词典，避免“重息壤”等被通用分词拆散。
- manifest 保存条目哈希和 chunk 键；增量构建只更新变化条目及分类 BM25，并校验三套索引一致性。

### 3.2 图谱建模与增量规则

- 实体覆盖人物、任务、地点、物品、设备、配方；名称、类型和来源可追溯。
- 关系只从结构化字段、配方结构或原文明确句式建立；边保存来源、证据和构建规则。
- 亲属、隶属、参与任务、生产/消耗等事实可入图；“喜欢、可爱、性格如何”等解释不固化成边。
- 同场出现仅可作为文本线索，不能自动等价为事实关系。
- 增量更新按来源内容哈希识别变化：先删除该来源旧边/派生实体，再重建，避免幽灵关系。
- 自动审查可逆问法，例如“庄方宜是不是武陵领袖”和“武陵领袖是谁”必须落到同一证据。

### 3.3 关系错误治理

1. 每条边必须能定位来源和证据；
2. 构建器用白名单规则，不允许 LLM 把猜测直接写入正式图；
3. `audit_relation_queries.py` 自动生成正向、反向和改写问法；
4. 错误应修正规则并按来源重建，不手改一条不可复现的数据库记录；
5. 图无路径只表示当前图谱无证据，不能断言现实中无关系，必须回退文本检索。

## 4. 在线问答与协作模式

```text
query → 意图识别 → 确定性路由优先
  ├─ 配方/设备：recipes 直查
  ├─ 枚举：分类/主题过滤
  ├─ 明确关系/多跳：图检索
  └─ 其他：RAG 多路检索
→ 图未命中回退 RAG
→ 复合/主观关系：事实图边 + 原文证据窗口
→ 证据阈值与覆盖检查 → LLM 据证生成或拒答
```

- 精确事实先结构化；能用配方表、分类或图边确定的内容不让 LLM 猜。
- 描述性内容回原文；性格、剧情、态度由 RAG 找证据，模型仅作有边界归纳。
- 事实与解释分开。例如“狼卫的妹妹是谁，她可爱吗”：图谱回答洛茜，原文支持主观说明；主观证据不足
  不能连带拒绝已经确定的姓名。
- 关系问题按需直接查图；图不足再走 RAG/mention，而不是机械混合图分和向量分。
- direct、graph、mention、keyword 和普通 RAG 命中保留类型标志，人工确认通道优先。

## 5. 当前是不是 Agent

不是完整 Agent，而是**确定性编排管线 + 可选 LLM 子步骤**。它没有通用工具协议、观察后的重新规划、
工作记忆或开放循环。

| 路径 | 典型 LLM 次数 | 说明 |
|---|---:|---|
| 配方/设备直查且不生成 | 0 | 完全结构化 |
| 结构化结果文本整理 | 0～1 | 仅需要自然语言时生成 |
| 普通开放 RAG | 通常 2 | 查询改写一次，最终回答一次 |
| 低置信意图 + 开放 RAG | 常见上限 3 | 意图兜底、查询改写、回答 |
| 图关系且路径明确 | 1 | 图查询无需 LLM，最后生成一次 |

失败或未配置 LLM 时会规则降级，因此实际次数可能更少。

### 5.1 Agent Loop 决策

当前不引入开放式 Agent Loop：它不会比脚本直查更快，每轮串行网络调用会增加延迟、不可复现路径和错误面；
现有 `query_expand → multi_search` 已覆盖一次规划式多路召回的主要收益；确定性工具本来也不需模型反复选择。

只有困难评测证明“第一轮证据不完整”是主要剩余错误时，才加入受限证据循环：

```text
检索 → 审查子问题覆盖率
  ├─ 足够：生成
  └─ 不足：LLM 从白名单选择一次补检索 → 再审查 → 生成或拒答
```

约束：最多一次补检索、JSON 参数校验、总超时、完整 trace、不写长期记忆、不允许模型修改图谱。
白名单仅含 graph、RAG、entity direct、mention/keyword、recipe 和枚举。上线门槛是困难集端到端正确率
显著提升且拒答正确率不下降，否则保持现状。

## 6. 长文档与所有截断预算审计

旧版 `gen_answer()` 每来源固定取开头 1500 字，答案在档案后段时永远不可见。现用
`focus_long_context()` 遍历全文句/行，以查询实体、关系词、评价词和实词评分，从不同位置选择最多 7 个窗口，
在每来源 1800 字预算内组合。

| 限制 | 目的 | 风险与处理 |
|---|---|---|
| 每来源 1800 字 | 控制 prompt | 查询驱动选段，不盲截开头；远端答案有回归测试 |
| 最多 7 个窗口 | 避免碎片过多 | 先为每个查询维度选最佳窗口，保护复合问题 |
| 默认生成 top-k=5 | 控制噪声 | 解释性复合关系至少 8；困难集决定是否调整 |
| 图路径最多 3 跳 | 控制路径爆炸 | 超限回退文本，不能解释为关系不存在 |
| 图边证据 500 字 | 明确句通常足够 | 保留来源；未来超长表格应改存 span/块 ID |
| 枚举整理最多 40 项 | 控制 prompt | API 返回完整 names；模型获得真实总数且必须声明仅展示前 40 项 |
| 前端摘要 260 字 | 仅预览 | 不影响后端生成 |
| API 媒体摘要 300/800 字 | 仅详情预览 | 不进入 `/api/ask` 生成上下文 |

剩余风险：词面窗口对远距离指代仍可能漏证；图证据 500 字不是原文 span。评测应加入答案在末尾、跨段指代、
双子问分散在首尾的样本。

## 7. 质量门禁与监控

- 检索：Recall@K、MRR、Precision@K，按问题类型分组；当前严格集 71 条 Recall@5=100%、MRR=97.3%。
- 图谱：实体/边数、孤立实体、悬空端点、来源缺失、正反问法一致率、关系评测。
- 端到端：忠实度、完整性、引用正确性、拒答正确率；检索 100% 不等于问答 100%。
- 运行：延迟、错误率、路由分布、空检索率、LLM 降级率、阶段耗时与调用次数。
- 变更：构建一致性、自动测试、检索评测、图评测、关系审查；指标回退阻止发布。
- 人工：高风险关系、困难复合问题和失败样本回看原文；LLM-as-judge 只能辅助金标。

本次验证：43 个单元测试全部通过；图评测 10/10；自动关系正向/反向/是非问审查 1656/1656；
总质量门禁通过。上述数字是固定规则和固定数据上的回归结果，不应表述为所有自然语言问题 100% 正确。

## 8. 维护顺序

1. 更新并提取规范化知识库；
2. 增量构建 RAG，验证 manifest/BM25/Chroma 一致；
3. 增量构建图谱并做关系审查；
4. 跑单测、检索评测、图评测和质量门禁；
5. 将失败归因到源数据、切片/召回、关系规则或生成；
6. 只有评测证明需要时才加 reranker、补检索循环或更大模型。

相关文档：`RAG_UPGRADE_PLAN.md`、`RAG_DEVLOG.md`、`output/RAG_TECHNICAL_OVERVIEW.md`、
`output/GRAPHRAG_ARCHITECTURE.md`、`scripts/README.md`。
