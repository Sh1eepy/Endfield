# WEB_UI_PLAN.md — 前端 UI 改版记录（终末地风格）

> 状态：✅ v1 与 2026-08 的工业档案 v2 均已完成。本文保留早期改版依据，不再作为待办清单。
> 当前实际界面与后续任务以 `web/index.html`、`PROJECT_STATE.md` 和 `PROJECT_PROGRESS.md` 为准。
> 目标：参考 https://endfield.hypergryph.com/#lore 的视觉语言，重做 `web/index.html`
> 的范围：配色 / 布局 / 组件 / 交互 / RAG 问答展示

---

## 一、现状与差距

| 维度 | 现状（浅色 SaaS 风格） | 官网风格（推测，见下文核实项） |
|---|---|---|
| 配色 | 靛蓝 #4F46E5 + 白底，通用管理后台感 | 终末地工业暗色 + 高对比警示橙/青，工业警示感 |
| 字体 | 系统字体 | 粗犷标题字 + 等宽数据字 |
| 质感 | 平面卡片 + 圆角 16px | 硬边面板 + 扫描线/网格纹理 + 警示条纹 |
| 氛围 | 明亮轻快 | 末世工业 / 战术 HUD |

**当前最突兀的点**：`#4F46E5` 靛蓝与终末地毫无关系；圆角卡片和"现代 SaaS 后台"气质与游戏世界观冲突。

---

## 二、设计方向（终末地工业风）

> 先打比方：现在的界面像"Notion 笔记页"，目标像"终端操作台的 HUD"——
> 不是满屏特效，而是**深色底 + 高对比信息层级 + 工业警示元素**。

### 1. 配色系统（CSS 变量重构）
```
--bg:      #0B0E13       深空黑蓝（页面底）
--panel:   #131720       面板底（略亮）
--panel2:  #1A2029       卡片/悬浮
--line:    #2A3342       描边（细线）
--text:    #E6EAF2       主文字（高亮白）
--sub:     #8A94A6       次要文字
--accent:  #FF8A3D       警示橙（主强调，终末地标志色）
--accent2: #4DD0E1       数据青（配方/数值）
--danger:  #FF4D4F       红（警告/不可用）
--ok:      #52C41A       绿（就绪）
```
配套：斜纹警示条（`.hazard`，黄黑斜纹）用于 header 或节点；扫描线网格背景；
标题用 `letter-spacing` + 少量倾斜；数据用等宽字（`Consolas/monospace`）。

### 2. 布局
```
┌──────────────────────────────────────────────┐
│ ▚▚ 终末地工业 · 合成终端        [状态灯●]     │  ← header：深色+警示条
├──────────────────────────────────────────────┤
│ [合成树] [知识问答]        ┌ 搜索框 ┐          │  ← 模式 tabs + 搜索
│ ┌────────────────────────────────────────┐   │
│ │   物品卡（封面 + 名称 + 分类徽章）        │   │
│ │   D3 合成树（橙色连线/节点，深色节点卡）   │   │
│ └────────────────────────────────────────┘   │
│ 引用 chips / 检索片段折叠（问答模式）         │
└──────────────────────────────────────────────┘
```

### 3. 组件改造清单
| 组件 | 现在 | 改成 |
|---|---|---|
| header | 白底渐变字 | 深色 + 底部 3px 黄黑警示条 + 等宽状态字 |
| 搜索框 | 白底圆角 | 深色面板 + 橙色 focus 描边 + 终端光标感 |
| 联想下拉 | 白底 | 深色面板 + 橙色高亮命中词 |
| 合成树节点 | 浅蓝圆点 | 深色节点卡 + 橙色物品环 + 青色配方标签 |
| 连线 | 灰色虚线 | 深青实线（物品→配方）+ 橙色（数量标签） |
| 引用 chip | 蓝底 | 青/橙双色标签 |
| 问答卡片 | 浅灰消息 | 深色卡片 + 等宽回答 + 引用角标 |

### 4. 交互增强（顺带做，不额外立项）
- 合成树节点**点击展开/收起**（现在只能悬停）
- 搜索历史（localStorage）
- 问答回答的 `[来源N]` 角标 → 点击滚动定位来源 chip（功能已写，待样式）
- 加载中状态（扫描动画条）

---

## 三、关于"要不要先做 skill 读网页设计风格"

**小鱼的建议：不需要专门做 skill，直接手工实现。** 理由：

1. **官网抓不到**：小鱼试过 `Invoke-WebRequest`（默认 UA 和浏览器 UA 都被 TLS 层断开），
   官网是 React SPA + 强风控，服务端拿不到 CSS token，skill 也拿不到。
2. **风格定位明确**：终末地是鹰角"末世工业"基调，官方公开素材（PV/海报/设定集）配色
   稳定在 `深蓝黑底 + 橙/青高亮`，不需要再逆向。
3. **正确姿势**：主人如果方便，**截一张官网 `#lore` 页面截图**丢给小鱼（截图比抓 HTML 直观
   一百倍），小鱼按截图精修配色；否则小鱼按上述"工业 HUD"方向先做一版，主人看完再调。

> 若主人坚持要 skill：可以做一个 `fetch-web-style` skill（用 playwright 渲染截图 + 提取 CSS 变量），
> 但那是"采集基建"，对本次改版不是必需，建议放最后做。

---

## 四、执行步骤（每步可独立验收）

```
第 1 步  重建配色变量 + header/搜索框/联想（深色基底）    ~0.5天
第 2 步  合成树 D3 节点/连线/图例重做（工业风）            ~0.5天
第 3 步  问答结果卡片 + 引用/片段样式统一                  ~0.5天
第 4 步  交互增强（节点展开收起/搜索历史/加载动画）        ~0.5天
第 5 步  全页面走查：对比官网截图（如有）微调              ~0.5天
```

总工作量约 2-3 天，全部只改 `web/index.html`（单文件，无构建）。

---

## 五、验收标准
- 深色主题下文字对比度达标（正文 #E6EAF2 on #131720，WCAG AA）
- 合成树在深色底上节点/连线清晰可辨，数量标签可读
- 问答回答、来源引用、检索片段在深色下层级分明
- 搜索联想、模式切换、节点交互无回归
