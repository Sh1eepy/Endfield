# 前端设计说明

## 方向

页面采用白色工业档案风格：黑色结构线、工程黄、克莱因蓝阴影和多边形切角。立体阴影与平面信息层叠，
但内容可读性优先，不让装饰遮挡表格、媒体和按钮。

## 页面结构

- 顶部：品牌、后端状态和简短说明；
- 搜索区：配方树/知识问答双模式，各自保存 query；
- 结果区：纵向配方树或带来源的知识回答；
- 干员档案：章节导航、局部 Tab、图片/视频卡、音频播放和独立表格滚动条；
- 背景：低透明角色、多边形和滚动视差；
- 页脚：图片素材与画师署名。

## 交互规则

- 空 query 显示入口和示例，不默认查询某个物品；
- 搜索建议前缀优先，再做包含匹配；
- 模式切换不自动发送请求；相同知识问题优先读本次会话缓存；
- 宽表自己横向滚动，不依赖整个网页底部滚动条；
- 图片保持可辨认尺寸，视频/图库不能塞进窄标题栏；
- 所有动画遵守 `prefers-reduced-motion`。

## 开场动画

开场约 3.76 秒。终末地图标在视口中心放大显示，斜向机械梁和锁销依次进入，进度从 0 到 100%，
短暂停留后释放主页面。进度是启动演出时间，不代表后端真实加载百分比；后端连接状态单独展示。

## 维护重点

- 核心实现集中在 `web/index.html`，修改 DOM id 前先看 `tests/test_frontend_contract.py`；
- 媒体通过 `/api/media` 代理时只允许可信 WIKI 域名；
- 新视觉效果要同时检查 1280×720、窄屏和减少动态效果模式；
- 素材路径使用 `/assets/...`，不要依赖开发机器的绝对路径。
