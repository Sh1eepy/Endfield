# RAG 开发记录

> 这里只记录会影响后续维护的决定。当前架构见 `KNOWLEDGE_SYSTEM_ARCHITECTURE.md`。

## 关键改动

### 流式输出与问答提速（2026-09）

- 新增 `POST /api/ask/stream`（SSE）：路由/检索与 `/api/ask` 共用 `rag_ask.ask_stream`，答案生成改为增量推送；
  事件 `phase → meta（意图+来源先亮）→ delta×N → done（完整结果）`，`done` 等价非流式返回体。
- `llm_client.chat_stream()`：OpenAI 兼容 `stream:true` 增量解析；首字前失败按退避重试、吐字后失败不重试；
  支持 `abort` 事件（客户端断开时在 chunk 边界中止，释放并发名额）。
- 生成预算由 800 提至 1600 tokens；流式/非流式读取 `finish_reason=length` 并自动续写一次，避免长档案回答静默截断或推理耗尽预算后正文为空。
- `prepare_generation()` 抽出 gen_answer 的确定性准备（拒答判断/上下文组装），流式与非流式共用，保证逐字一致；
  确定性枚举无需 LLM，直接返回完整 `done`。
- 启动预热：`start_server.py` 单进程默认预加载 embedding 模型与索引（`RAG_PREWARM=0` 关闭），
  把冷启动从首个问答请求移进健康检查 start_period。
- 旧 `/api/ask` 保持不变（小程序、评测、门禁继续使用）；web 前端优先走流式，旧后端自动回退整包。
- 前端增量渲染按 80ms 节流，避免逐 token 全量重渲染 markdown；Nginx 部署须对 `/api/ask/stream` 关 `proxy_buffering`。
- 说明：流式改善的是**感知延迟**（等待从"整段生成完"变成"首字后持续流动"），不减少最后一个字的完成时间；
  真正减总耗时的一步（简单实体问题跳过 LLM 语义规划）仍按门禁要求评估后再决定是否启用。

### 索引和检索

- 给 chunk 加“分类 + 条目名”前缀，并生成游戏专名词典。严格检索集 Recall@5 从 72% 提升到 85%。
- 修复 `sections` 为空时不产生 chunk 的问题，恢复 53 个长条目和 370 个 chunk。
- 加入名称检索并修正评测集字符归一化，当前 71 条集 Recall@5=100%、MRR=97.3%。
- 增量构建会校验 BM25 分片，自动修复缺失、陈旧、损坏和多余分片。
- 长条目即使已有 `sections`，也会补索引 `full_text` 中未覆盖的描述/其他内容；修复莱万汀冰淇淋档案漏切片。
- `operator_details.json` 的中文语音台词以独立来源进入 RAG；当前 2373 条，其中 3 条含冰淇淋线索。
- 条目 hash 加入 sections 与索引策略版本；Chroma 全量写入改为分批，切分升级和大索引都可正确重建。

### 问答路由

- 配方和设备使用结构化直查，不让语义检索猜结果。
- 明确实体可直接取完整条目；“有哪些”按分类枚举；间接条件使用关键词、mention 和查询改写。
- 知识图谱处理明确关系和多跳路径；图里没有证据时回退 RAG。
- “喜欢、可爱、性格”等解释性问题不写入事实图，使用原文窗口支持有边界的归纳。
- 态度关系窗口要求同时命中具体主体与对象/亲属线索，并按来源去重；修复“只出现管理员”的无关语音占满上下文。
- 开放问题用一次受约束 LLM 生成语义检索计划，合并原来的低置信意图兜底与查询改写；规划器不回答事实。
- 关系路由要求存在人物/组织/亲属对象，“喜欢吃什么”识别为对象偏好并走全文、语音和混合 RAG。

### 长文档和复合问题

- 旧版生成上下文固定截取每个来源开头 1500 字，导致后半段答案不可见。
- 现用 `focus_long_context()` 从全文不同位置选择与问题相关的窗口，并优先覆盖多个子问题。
- “狼卫的妹妹是谁，她可爱吗”推动了两项修复：明确亲属边负责事实，原文证据负责主观说明；
  某个子问题缺证据时不能拒绝整道题。
- 枚举结果由后端确定性生成完整清单，不再经过 LLM；修复默认 40 项截断及生成 token 二次截断。

### 图谱和质量

- 图谱边保存来源、证据和提取规则；错误应修规则后重建，不直接手改数据库。
- 增量更新按来源哈希删除旧关系再重建，避免残留关系。
- 当前图评测 10/10，关系正向、反向、是非问审查 1656/1656。
- 检索 100% 只代表固定集候选召回，不代表端到端答案 100%。

### Agent Loop 决定

当前保持确定性路由，不加入开放工具循环。普通开放问题通常是一次语义检索规划加一次答案生成，明确图关系
通常只需一次生成，结构化直查可以零 LLM 调用。

如果困难集证明主要错误来自第一轮证据不完整，再增加最多一次的受限补检索。工具必须白名单化、参数用
JSON 校验，并记录完整 trace。没有评测收益就不增加这层复杂度。

## 维护提醒

- `.env` 不提交；模型和 embedding 离线加载；
- 数据更新后依次重建 RAG、mention 和图谱，再跑全部门禁；
- 新失败问题先判断是源数据、召回、图关系还是生成问题，不要直接调模型参数；
- reranker 和更大模型都要先用困难集证明收益。
