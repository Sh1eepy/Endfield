# RAG_DEVLOG.md — RAG 问答开发日志与技术决策

> 状态：持续维护（2026-08）
> 目的：记录本项目 RAG 问答系统的**每次改动、遇到的问题、解决方法、选择理由**，
> 供后续维护/接手时理解"为什么是现在这样"。
> 阅读顺序：先 `RAG_UPGRADE_PLAN.md`（计划/架构）→ 本文件（决策/踩坑记录）→ `scripts/README.md`（工具用法）

---

## 一、决策总览（为什么这样设计）

| # | 决策 | 理由（一句话） |
|---|---|---|
| D1 | 评测集先行（阶段 1 先建 71 条测试集） | 没有尺子就无法判断改动好坏，后续所有优化都靠它验证"指标只升不降" |
| D2 | 四层路由：枚举 → 结构化直查 → 多路检索 → 实体直取 | 不同问题形态需要不同检索策略，一锅炖必然两头不讨好 |
| D3 | 配方类走结构化直查而非语义检索 | `recipes.json` 是精确数据（345 配方），直查命中率 100%，不该让语义猜测 |
| D4 | 检索增强用「jieba 词典 + chunk 元信息前缀」而非 rerank | 两个手段零成本、立即见效（Recall 72%→85%），rerank 留作可选进阶 |
| D5 | LLM 角色 = 整合与呈现，知识库角色 = 提供事实 | LLM 只能整合"喂到嘴边的信息"，检索必须先把事实找全，否则 LLM 要么拒答要么编造 |
| D6 | 密钥走 `.env`/环境变量，代码零明文 | 防止密钥入库泄露（补了项目原本缺失的 `.gitignore`） |
| D7 | 新增轻量 GraphRAG，关系题路由到 SQLite 图谱 | 多跳关系不应依赖“多搜几篇让 LLM 猜”；图定位、原文取证、无边回退 |
| D8 | 正式图只收高可信结构关系，派生关系查询时计算 | 降低错误固化与增量依赖重算成本；LLM 抽取候选必须经审查 |
| D9 | 关系覆盖按分类字段建模，并自动生成正/反/是非问审查 | 防止针对单个问题补丁化修复和查询方向不对称 |
| D10 | 主观关系使用图定位+原文窗口+受约束解读 | “喜欢/性格”不是稳定事实边，既要利用剧情证据也要避免污染图谱 |

## 一·一、GraphRAG v2：从单点关系修复扩展到全域关系

- 将章节 entry 引用从统一 `REFERENCES` 拆为任务奖励/解锁、干员材料/武器推荐、物品来源/用途等语义边；
- 将 345 条配方转为设备使用原料和生产物品两类边，证据保留配方 ID、数量与耗时；
- 只对明确“管代”职称句式抽取 `AUTHORITY`。庄方宜样例有“武陵科学发展区管代”和
  “我是武陵的管代”双证据，“带领天师队伍”不作为地区职务证据；
- 新增 `audit_relation_queries.py`，对真实边批量生成正向、反向、是非三种问法并纳入门禁；
- “诀是否中意管理员”走 `hybrid_relation`，提取任务/档案/对话局部窗口，答案必须区分事实与解读，
  且任何解读都不回写正式图谱。

## 一·二、前端查询缓存与媒体表格修复

- 知识问答结果按当前页面会话中的 query 缓存；配方/问答模式切换只重新渲染缓存，只有 query
  实际变化才重新请求 `/api/ask`，避免重复消耗一次 LLM 调用与等待时间；
- WIKI 的干员履历、战斗 GIF、档案回顾和塔卫二记事社素材常以“表格单元格内图片”保存。旧前端
  将所有单元格图片统一渲染为 `1.25em` 行内图标，造成整张素材被压缩；现按内容识别媒体型表格，
  改为自适应大图画廊。升级材料等数据表中的物品图标仍维持紧凑尺寸。

## 一·三、复合事实与主观评价问题

真实失败样例“狼卫的妹妹叫啥来着，她是不是很可爱”暴露了两个问题：实体全文虽然包含“洛茜”，
但关系证据位于生成上下文截断位置之后；“可爱”也未触发解释性关系路由。修复后：

- 从人物简介的明确“X 的妹妹/哥哥”句式建立可追溯亲属边，当前 `狼卫 ←妹妹— 洛茜` 的证据来自
  洛茜简介，不以同场出现推断亲属；
- “可爱/漂亮/帅/有趣”纳入解释性词，关系证据窗口对查询中出现的亲属词加权；
- 复合问题先提供图谱事实，再提供人物原文供主观解读；生成提示要求逐项回答，某个主观小问证据不足
  不能连带拒绝已经有明确证据的姓名小问。

### 根因补修：生成上下文不再固定截取正文开头

进一步审查确认，亲属边只能修复已建模关系，无法解决所有长档案问题。真正的通用缺陷位于
`gen_answer()`：此前每个来源固定使用 `h['text'][:1500]`，因此实体直取虽然拿到了完整档案，位于
1500 字之后的答案仍永远不会进入 LLM。

现改为查询聚焦上下文：对整篇全文分句/分行，以专有实体、问题实词、关系词和评价词评分；先为
每个查询维度选择最佳窗口，再补充综合得分窗口，按原文顺序组合。它可以从开头、中段和末尾同时
取证，复合问题不会只覆盖一个子问；完全没有匹配窗口时才回退开头。回归测试人工构造了答案位于
正文 1500 字以后的长档案，要求在 500 字上下文预算内仍必须找到远端答案。

---

## 二、阶段 0-5 开发中的问题与解法

### 阶段 0：在线 LLM 接口

| 问题 | 现象 | 解法 | 理由 |
|---|---|---|---|
| P0-1 初始化顺序 bug | `llm_client` 报 `AttributeError: 'LLMClient' object has no attribute 'model'` | `__init__` 里先赋值 `self.model` 再调用 `_guess_base_url()` | base_url 猜测依赖 model 名，顺序颠倒 |
| P0-2 `.env` 读取异常 | PowerShell `Get-Content` 正则读 `.env` 只显示 2 个键，但 Python 能读到 3 个 | 改用 Python 直接检查；`llm_client` 自带 `_load_dotenv()` | 终端编码/BOM 干扰正则，Python 读 UTF-8 可靠 |

### 阶段 1：评测集

| 问题 | 现象 | 解法 | 理由 |
|---|---|---|---|
| P1-1 生成器缺数据源 | `gen_eval_set.py` 报 `KeyError: '比较'` | `anchor_specs` 补充"比较"类数据源 | 比较对需要两个锚点，之前漏了构造 |
| P1-2 命中判定放水 | 旧 `eval_rag.py` 用 `t in name` 子串匹配，查"电池"命中"中容谷地电池"算对 | 新 `eval_retrieval.py` 严格判定（归一化后相等或互为简称包含） | 指标必须真实反映检索能力 |
| P1-3 名称字符差异 | 全角括号 `管理员（男）` vs 半角 `管理员 (男)`、引号 `安"桩"不易` vs `安桩不易` 匹配失败 | 判定前 `_norm()` 归一化（全角→半角、去引号空格） | 字符形态差异不是检索问题，不应算失败 |

### 阶段 3：结构化直查路由

| 问题 | 现象 | 解法 | 理由 |
|---|---|---|---|
| P3-1 整句当物品名 | `recipe_lookup("重息壤怎么合成")` 拿整句查配方库匹配不到 | 加 `extract_item_name()` 从查询抽实体名（最长包含匹配） | 自由文本查询 ≠ 精确名称，需先抽取 |
| P3-2 纯设备名被跳过 | 「天有洪炉」意图识别为"知识"（无关键词特征），没走设备直查 | `ask` 入口对纯名称查询（`extract_item_name(q)[0] == q`）也走直查 | 用户直接输入设备名就是想要它的配方 |
| P3-3 设备产物反查缺失 | 「什么设备能生产电池」——"电池"是简称，配方库无此名 | 正则提取"生产X"的产物关键词，反查 recipes outputs | 查询描述的是"产出物"而非"设备名" |
| P3-4 短名歧义 | 「灼铜」匹配 4 个物品（灼铜块/气态灼铜…） | `find_item_ids_by_name` 子串回退 → 返回候选列表 | 歧义时让用户选，不猜 |

### 阶段 4：检索增强

| 问题 | 现象 | 解法 | 理由 |
|---|---|---|---|
| P4-1 环境 Python 缺依赖 | 系统 python (3.14) 无 `rank_bm25/chromadb` | 确认项目用 conda `endfield` 环境（`C:\Users\28277\miniconda3\envs\endfield\python.exe`） | AGENTS.md 记录的环境，依赖都装在那边 |
| P4-2 索引重建文件锁 | `PermissionError: data_level0.bin`（uvicorn 锁住 ChromaDB） | 先停 API 服务 → 重建 → 再启动 | Windows 文件锁，运行中的进程占用 |
| P4-3 专有名词被切碎 | jieba 把"重息壤/向心之引"切成多段，BM25 失配 | `gen_jieba_dict.py` 自动生成 1789 词自定义词典 | 游戏名词不是通用词，词典让分词正确 |
| P4-4 chunk 丢语境 | 短条目（如"负山"）检索不到 | chunk 文本加元信息前缀 `【分类】名称：名称。正文`（Contextual Retrieval 轻量版） | 名称直接参与 BM25/向量匹配 |

**效果**：Recall@5 **72%→85%**，数值类 24%→81%（词典+元信息，两个免费手段）。

### 评测迭代

| 步骤 | 改动 | 效果 |
|---|---|---|
| 基线 | 旧评测集（79 条随机配对） | 72% |
| +词典+元信息 | 检索增强 | 85% |
| 比较类同类配对 | 随机配对（头像 vs PV 无意义）→ 同分类配对 | 89% |
| +系列命中判定 | 「跨过寰宇深处」用户指系列，gold 却精确到"·二" → 前缀匹配 | 93% |
| +名称归一化 | 全角/半角/引号差异 | 稳定 93% |

---

## 三、主人实测暴露的三大问题（重点）

> 这三类问题不是代码 bug，而是**架构上缺了对应的问题处理通道**。
> 每个都体现了"知识库有数据但检索捞不到 → LLM 有嘴也答不出"的共同模式。

### 问题 A：「诀从一级升到满级要多少材料」

**现象**：查「诀」能出来，但问"诀升满级要多少材料"失败（拒答）。
**根因**（三层）：
1. 实体抽取只认配方库——「诀」是**干员**，不在配方库 → 抽不出实体
2. RAG 检索被"升级/材料/满级"带偏——`诀(干员)` 排到 top6，被挤出 top5
3. chunk 切分丢数据——精英化材料表格在后面的 chunk，LLM 看不到

**解法**（逐层修复）：
1. `extract_kb_entity()`：实体抽取扩展到**知识库条目名**（干员/武器/装备/任务）
2. 主条目判定改为**精确同名**（"诀的信物"是独立条目不是衍生，不能算"诀"命中）
3. **实体直取**：抽到实体 → 直接取该条目**完整全文**当上下文（绕开 chunk 切分），
   再补上检索结果里的其他相关条目（去重合并）

**为什么选实体直取而非别的**：
- 相比 rerank：实体已确认存在，全文就是权威上下文，不需要"排序猜"
- 相比多轮检索：单实体问题一轮就能定位，多轮是给开放问题用的

**结果**：LLM 给出完整四阶段精英化材料表 + 总计（折金票×126100 等），来源直指「诀(干员)」。

### 问题 B：「终末地至今为止的主线任务有哪些」

**现象**：枚举类问题（"有哪些"）完全失败——top8 全是武器/装扮/活动，69 条主线任务一条都捞不到。
**根因**：纯向量检索对**枚举/聚合**问题失效。查询里没有实体名，且"主线/任务"这类泛词会被 BM25 高权重词（武器名等）淹没。
**解法**：新增**枚举通道**——
- 识别"有哪些/列举/所有"模式 → 按主题关键词在知识库**分类内过滤**枚举
- 主题即分类（干员/武器/活动）：直接枚举该分类全部条目
  （因为条目名是"佩丽卡/四二式·肃阵"，本身不含"干员/武器"字样）
- 主题是分类子集（主线任务 ⊂ 任务）：按名称关键词过滤（"第一章/进程"）
- LLM 负责分组整理（序章 2 项 + 第一章 29 项按进程分组）

**为什么选分类枚举而非 RAG**：枚举是"确定性问题"（列出所有X），分类过滤是精确操作；
RAG 是"相似性问题"，用语义检索做枚举既慢又漏。

**结果**：主线任务/干员/武器/活动全部能列出，LLM 自动分组编号。

### 问题 C：「解锁武陵地区需要满足什么条件或者做什么任务」（开放/前置问题）

**现象**：既无实体名（"武陵"是地名不是条目名），信息又间接（解锁条件写在**别的任务条目**里）。
**根因**：
1. 地名非条目名 → 实体抽取失效
2. 解锁条件在"第二章-进程Ⅰ-初探武陵"等任务条目里，单次搜"武陵"捞不到
3. 纯 RAG top-k 里混入大量无关条目（含"武陵"字样的干员设定）

**解法**（③ + ② 组合）：
1. **③ mention 反查索引**：扫描知识库建"谁提到了 X"索引（1708 名/12632 关系），
   缓存到 `output/mention_index.json`
2. **全文关键词检索**：`keyword_search()` 按词列表在条目名+全文匹配，
   **分类加权**（任务/活动优先，干员/武器降权——"诀/弭弗"人物设定带"武陵"但无关）
3. **② 查询改写**：LLM 把模糊问题拆成多个子查询（"武陵 解锁""武陵 条件"…）
4. **多路检索合并**：原查询 + 子查询 → 各走 RAG/全文关键词/mention → 合并去重，
   **人工确认片段（keyword/mention/direct）排前面**，RAG 结果靠后
5. **拒答阈值修正**：多路片段是人工确认的相关上下文，不因 top-1 vec 低被拒答

**为什么选多路检索而非单路**：开放问题的信息是**间接且散落**的（任务链横跨多个条目），
单次检索只能命中其中一环；多路并进才能把线索凑齐，再交给 LLM 整合。

**结果**：LLM 给出完整解锁路径：
> 1. 完成 第一章-进程Ⅳ-准备工作（前置）→ 2. 第二章-进程Ⅰ-初探武陵 → 3. 误入清波寨 → 4. 前往武陵城
> 且诚实区分"明确解锁条件"和"推断出的解锁路径"。

---

## 四、关键设计理由详解

### 为什么 LLM 不能直接答，必须配检索？
主人原话质问："知识库如果只是检索工具我还需要 LLM 干嘛"——**这个质疑是对的**。
LLM 是回答者，但它**只能基于上下文回答**。如果上下文（检索片段）里没有
"精英化材料表/解锁条件"，LLM 只有两条路：
1. 拒答（"未找到"）——诚实但答不出
2. 编造（幻觉）——更糟
所以检索的责任是**把答案的原材料找全**，LLM 的责任是**整合成人类可读的回答**。
两者缺一不可，这是 RAG 的立身之本。

### 为什么"检索不到"是主因而不是"逻辑不足"？
主人最初担心是逻辑问题。实测证明：知识库里**有**全部答案（精英化表格、解锁条件、
主线任务清单），问题都在"检索捞不到"。所以解法聚焦检索策略（实体直取/枚举/多路），
而不是让 LLM 更聪明。

### 为什么四层路由而不是一套通用检索？
| 问题形态 | 例子 | 最优策略 |
|---|---|---|
| 精确实体 | 重息壤怎么合成 | 结构化直查（100% 命中） |
| 单实体但被噪声干扰 | 诀升满级材料 | 实体直取（完整全文） |
| 枚举 | 主线任务有哪些 | 分类枚举（确定操作） |
| 开放/前置/间接 | 解锁武陵 | 多路检索（凑齐散落线索） |

一套通用 RAG 检索无法同时做好这四件事——分类处理是"真正有用"的关键。

---

## 五、已知限制与未来可选项

| 限制/可选 | 说明 | 优先级 |
|---|---|---|
| 双实体召回弱 | 比较类困难档（两个实体都要命中 top-k）仍有失败 | 中 |
| rerank 精排 | cross-encoder 可进一步提升排序质量（召回 top-50→精排 top-5） | 低（当前 Recall 已 93%） |
| 评测集人工抽检 | 自动生成的评测集 gold 偶尔偏差（如"佩丽卡攻略"），需主人抽检修正 | 中 |
| 地名表固化 | `PLACE_WORDS` 是手工维护的地名表，新地图需补充 | 低 |
| mention 索引重建 | 知识库更新后需重建 `output/mention_index.json`（build_mention_index(force=True)） | 低 |
| 多轮对话 | 查询改写带上下文（"它"指代上一轮物品） | 低 |

---

## 五·一、2026-08 收尾审计：名称召回与漏索引修复

失败样本最初集中在“定档PV”“佩丽卡怎么玩”“清淤”等查询。审计发现两层根因：

1. 混合检索缺少条目名称核心词通道，短名容易被高频语义结果淹没；
2. 更严重的是，53 个全文很长但 `sections` 为空的条目被 `split_chunks()` 返回零块，玩家攻略实际从未进入索引。

处理方式：

- `rag_search.py` 新增名称召回，过滤“攻略/PV/视频”等意图通用词，使用标题核心实体匹配；
- 明确攻略/视频意图时提升名称+分类强证据，普通查询仍走均权 RRF；
- `build_rag.py` 对无 sections 的长条目回退切分 full_text；
- 增量构建增加 manifest/BM25 chunk 键一致性校验，自动修复陈旧、缺失、损坏和废弃分片；
- 人工确认“佩丽卡怎么获得”的 gold 应为干员本体，修正该条标注。

结果：新增 370 chunks，索引 3032 → 3402；71 条严格评测 Recall@5 92.96% → 100%，MRR 89.48% → 97.3%。
当前没有必要引入 reranker，未来应先扩充并人工复核评测集。

## 六、文件变更清单（本次 RAG 改造）

**新增**：
- `scripts/llm_client.py` — 在线 LLM 客户端
- `scripts/intent_router.py` — 意图识别
- `scripts/rag_ask.py` — 问答路由（枚举/直查/多路/实体直取）
- `scripts/gen_eval_set.py` — 评测集生成器
- `scripts/eval_retrieval.py` — 评测脚本
- `scripts/gen_jieba_dict.py` + `scripts/dict_zh.txt` — jieba 词典
- `.gitignore` / `.env.example` — 密钥安全
- `output/eval/` — 评测集与历次结果
- `output/mention_index.json` — mention 反查索引

**修改**：
- `scripts/api_server.py` — 加 `POST /api/ask`
- `scripts/build_rag.py` / `scripts/rag_search.py` — 词典加载 + chunk 元信息
- `web/index.html` — 双模式 + 深色工业 HUD
- `RAG_UPGRADE_PLAN.md` / `scripts/README.md` — 进度与用法
