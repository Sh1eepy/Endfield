# PROJECT_PROGRESS.md — 收尾实施记录

> 用途：记录每个阶段的重要决策、实际改动、验证方法和后续维护方式。
> 最新总体状态仍以 `PROJECT_STATE.md` 为准；技术细节分别见 `DEVELOPER_GUIDE.md` 与 `RAG_DEVLOG.md`。

## 总体路线

| 阶段 | 内容 | 状态 |
|---|---|---|
| 1 | 文档统一、敏感文件审计、Git 基线 | ✅ 完成 |
| 2 | 后端/API/合成树自动化测试 | ✅ 完成 |
| 3 | 前端交互完善与验证 | ✅ 完成 |
| 4 | RAG 弱项优化与评测集抽检 | ✅ 完成 |
| 5 | Docker、部署配置与端到端验收 | 待开始 |

## 阶段 1：文档与 Git 基线

### 重要决策

1. 保留 `RAG_DEVLOG.md` 等历史决策记录；只修正会误导接手者的旧现状描述。
2. `.env`、私钥和本地环境文件永不提交；提交空值的 `.env.example` 作为配置模板。
3. 147MB 原始抓取 JSON、webpack `chunks/` 和根目录历史导出物保留在本地，但不进入普通 Git 历史。当前流水线以 `endfield_kb/`、`output/` 和 `scripts/` 为准。
4. `output/rag/chroma/` 是可重建的本地向量数据库，继续忽略；可审阅的 manifest、BM25 分片和评测结果保留版本管理。
5. `wiki数据采集skill/` 是独立旧工作副本，保留本地但不并入主仓库，避免两套说明和产物发生漂移。

### 已处理

- 统一文档中的前端描述为“深色工业 HUD + 配方树/知识问答双模式”。
- 更正当前索引数量与检索评测指标。
- 扩充 `.gitignore`，避免密钥、大型原始数据、逆向缓存和编辑器配置误入仓库。

### 验证与维护

- 用 `git check-ignore -v .env` 确认真实密钥文件被忽略。
- 用 `git status --short` 审阅首次基线中将被提交的文件。
- 新增环境变量时只更新 `.env.example` 的变量名和说明，不填写真实值。
- 原始 WIKI 数据更新后依次运行知识库、配方、媒体和 RAG 构建脚本；不要强行提交大型原始 JSON。

### 阶段结论

文档口径和实际代码已经对齐；敏感文件与大型可重建数据均已验证不会进入版本库；仓库使用 GitHub noreply 邮箱建立首次可回滚基线。后续阶段应按功能拆分提交，避免再次形成无历史的大批量改动。

## 阶段 2：后端与合成树自动化测试

### 重要决策

1. 使用 Python 标准库 `unittest`，不新增 pytest 依赖，保证项目在现有离线环境可直接测试。
2. 测试直接调用服务函数和本地数据，不要求先启动 uvicorn；问答入口使用 mock，禁止消耗真实 LLM Key。
3. 除典型查询外，遍历当前全部真实配方产物，验证所有成功生成的树都满足“叶子是基础资源、深度不超过 10、每节点配方不超过 2”的不变量。

### 发现并解决

- 空白搜索词原本会触发名称子串匹配，造成几乎所有物品都成为歧义候选。现在 `/api/synthesis` 在加载配方前拒绝空白输入。
- 负数 `max_depth` 没有明确语义，现在入口直接返回参数错误。

### 验证与维护

- 运行 `python -m unittest discover -s tests -v`。
- 运行 `python -m compileall -q scripts tests` 做语法检查。
- 修改配方选择、叶子判定、循环剪枝、API 返回结构时，必须同步补充或更新 `tests/test_api_server.py`。

## 阶段 3：前端交互完善

### 重要决策

1. 展开/收起使用节点稳定键保存当前树的临时状态，每次切换都重新计算 D3 布局；新查询清空折叠状态，避免跨物品污染。
2. 圆点负责展开/收起，物品文字负责跳转查询，两个操作的点击区域分离。
3. 最近 8 条查询只保存在当前浏览器的 `localStorage`，不发送到后端；配方和问答模式分别记录，可由用户一键清除。

### 发现并解决

- 原渲染器是一次性静态绘制，现已支持任意非叶子物品节点的展开/收起。
- 树内物品名称现在可直接切换到该物品的合成树。
- 新增配方/问答搜索历史及模式恢复。
- 修复已有结果时切换模式访问不存在的 `#syn-empty` 导致前端异常的问题。

### 验证与维护

- 用 Node 解析内联脚本，确认 JavaScript 语法有效。
- 在本地浏览器实际验证：177 节点的“重息壤”树收起为 1 节点后可完整恢复；子物品跳转和历史记录正常；模式切换无控制台错误。
- 修改树节点 DOM 时应保留 `.node-label`、`data.key` 和 `_hasChildren` 语义；修改历史结构时需更换 `HISTORY_KEY` 版本，避免旧数据不兼容。

## 阶段 4：RAG 弱项优化与评测审计

### 重要决策

1. 先审计失败样本，不直接引入 cross-encoder reranker；区分真实召回缺陷和错误 gold，避免为指标过拟合。
2. 增加轻量名称召回，与向量、BM25 做 RRF；“攻略/怎么玩”和“PV/哪里看”等明确意图才提升名称+分类的强证据。
3. 评测集中“佩丽卡怎么获得”应命中干员本体，不应命中玩家攻略，已人工修正 gold 并标记 `manual_review`。
4. 索引更新必须校验 manifest 与 BM25 分片的一致性，发现缺失、陈旧、损坏或废弃分片时自动重建对应分类。

### 发现并解决

- `split_chunks()` 对“全文超过 512 字且 sections 为空”的条目返回零块，导致 53 个长条目（含玩家攻略）静默缺失。现回退按 `full_text` 行/段落切块。
- 增量构建此前只比较知识库与 manifest hash，不能发现陈旧 BM25 分片；现增加分类 chunk 键一致性检查和自愈。
- 名称召回排除“攻略/PV/视频”等通用意图词，使用标题核心实体，避免所有攻略或视频互相干扰。
- 修复后增量加入 370 chunks，总数从 3032 增至 3402，更新 9 个 BM25 分类。

### 验证与维护

- 18 个 `unittest` 测试通过，包括名称核心召回、攻略优先和 BM25 分片自愈。
- 71 条严格评测：Recall@5 从 92.96% 提升到 100%，MRR 从 89.48% 提升到 97.3%，结果存于 `output/eval/final_reviewed.json`。
- 知识库更新后运行 `build_rag.py --incremental`；即使内容 hash 未变化，也会修复不一致分片。
- 当前数据集已满召回，不引入 reranker；未来扩充并人工审核评测集后，如排序质量下降再评估。
